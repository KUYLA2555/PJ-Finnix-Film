# docs/

| ไฟล์ | บทบาท |
|---|---|
| `master-prompt.md` | บรีฟหลัก v1.0 — **เป็นข้อผูกมัด** งานทุกชิ้นถูกตัดสินด้วยข้อที่มีหมายเลข 1–12 · ข้อ 2 คือ anti-brief ถ้าละเมิดคือรื้อทำใหม่ |
| `CHANGELOG.md` | บันทึกการตัดสินใจทุกเวอร์ชัน · เพิ่มหัวข้อใหม่ไว้**บนสุด** · ท้ายไฟล์มี "ค้างอยู่" กับ "ตัวแปรที่ต้องขอ" ที่ต้องอ่านก่อนเริ่มงานเสมอ |
| `film-data.md` | ค่าสเปกจริงทั้ง 15 รหัส + ข้อค้นพบที่ใช้เป็นเนื้อหาได้ + ข้อที่ต้องยืนยันกับลูกค้า |
| `review.html` | หน้าตรวจงานภายใน `noindex,nofollow` **ห้ามขึ้นเซิร์ฟเวอร์จริง** · ค้างที่ v1.2 ตามหลังหลายเวอร์ชัน อย่าใช้ตัดสินสถานะปัจจุบัน |

เปิด `review.html` ด้วย local server จาก root ของโปรเจกต์ — `http://localhost:8000/docs/review.html`
