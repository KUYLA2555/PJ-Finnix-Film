# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ภาพรวม

เว็บไซต์ Brand & Corporate ของ บริษัท ฟินนิกซ์ ฟิล์ม จำกัด (ฟิล์มกรองแสงรถยนต์) — static site ล้วน
**ไม่มี build system · ไม่มี package manager · ไม่มี test** หนึ่งหน้า = หนึ่งไฟล์ HTML ที่มี CSS และ JS อยู่ในไฟล์เดียวกัน

## โครงสร้างโฟลเดอร์ (จัดใหม่ใน v1.9)

```
*.html      6 หน้าจริงที่ root — หนึ่งหน้าจบในไฟล์เดียว ห้ามแยก CSS/JS (ข้อ 9)
assets/     ภาพที่เว็บเรียกใช้ + ภาพอ้างอิงภายใน
data/       films.json (ตัวเลขสเปก) + dealers.json (รายชื่อตัวแทนบน /contact) — source of truth ทั้งคู่
scans/      สแกนโบรชัวร์ 17 หน้า แยกตามซีรีส์ (ถอดเป็นข้อความครบแล้ว)
docs/       บรีฟ · CHANGELOG · film-data · review.html
tools/      sync-shared.py + shared/ — ฉบับจริงของ "ของร่วม" (v3.3) ไม่ใช่ build system
```

ทุกโฟลเดอร์มี `README.md` ของตัวเอง — เปิดอ่านก่อนแตะของข้างใน
**`Data/` เดิมถูกแยกเป็น `data/` กับ `scans/`** เพราะ Windows มองว่า `Data/` กับ `data/` เป็นโฟลเดอร์เดียวกัน · ถ้าจะย้ายโฟลเดอร์ที่ต่างกันแค่ตัวพิมพ์อีก **ต้องทำสองจังหวะ** (ย้ายของออกก่อน แล้วค่อยเปลี่ยนชื่อ) ไม่งั้นดัชนี git จะมีทั้งสองชื่อพร้อมกันแล้วแตกบน Linux/CI

**เริ่มใช้ git แล้ว 8 ส.ค. 2569** — `KUYLA2555/PJ-Finnix-Film` (**private**) · ประวัติก่อนหน้านั้นไม่มี ต้องอ่านจาก `docs/CHANGELOG.md` แทน
**อย่าสับสนกับ `KUYLA2555/ProjectWorldFilm`** ซึ่งเป็นคนละโปรเจกต์ — เว็บ WORLD FILM ศูนย์ติดตั้งที่ขายหลายแบรนด์ (มี Finnix เป็นแบรนด์หนึ่ง) เป็น public และ deploy ขึ้น GitHub Pages ทุกครั้งที่ push เข้า `main` · **ห้าม push ข้ามไปที่นั่นเด็ดขาด**
การเชื่อมต่อ push ก้อนใหญ่เกิน ~2 MB ไม่ผ่าน (HTTP 408) — ถ้าต้องเพิ่มไฟล์ภาพ ให้แยก commit ทีละไฟล์
**line ending ไม่ตรงกันทั้ง repo และยังไม่มี `.gitattributes`** — **ในดัชนี git ทุกไฟล์เป็น LF หมด** ที่ต่างกันคือ working tree — **6 หน้าที่ root เป็น CRLF ทั้งหมด · `docs/review.html` เป็น LF** (ดูด้วย `git ls-files --eol` คอลัมน์ `i/` กับ `w/` · วัดซ้ำ 25 ส.ค. 2569) · `core.autocrlf=true` มาจาก gitconfig ระดับเครื่อง **ไม่ใช่ค่าของ repo** คนอื่นที่ clone ไปจึงได้ผลต่างกัน
อาการจริงคือ git เตือน `LF will be replaced by CRLF the next time Git touches it` ทุกครั้งที่แตะสองไฟล์นั้น — **`checkout` / `reset` / `stash` รอบหน้าจะเขียนทับทั้งไฟล์ใน working tree** ส่วน `git diff` ไม่พัง เพราะ autocrlf normalize ให้ทั้งสองทางอยู่แล้ว
**วัดแล้วว่าข้อเตือนเดิมที่ว่า "แก้ไฟล์ LF ทีหนึ่ง diff จะขึ้นทั้งไฟล์และกลบของจริงหมด" ไม่เกิดขึ้นจริง** (20 ส.ค. 2569 — ตอนนั้นวัดกับ `products.html` ที่ถูกลบไปแล้ว) อย่าเสียเวลาไล่แก้อาการนี้
**กับดักตอนแก้ไฟล์ด้วยสคริปต์ (เหยียบมาแล้วใน v3.2)** — เวลาประกอบข้อความด้วย Python **อย่าเรียก `.replace` แปลงตัวขึ้นบรรทัดเป็น CRLF กับสตริงที่ถูกแปลงมาแล้วรอบหนึ่ง** จะได้ `CR CR LF` แทน `CR LF` ซึ่งทำให้ `git ls-files --eol` เปลี่ยนจาก `w/crlf` เป็น **`w/-text`** (git มองว่าไม่ใช่ text ล้วน) และ diff จะสกปรกทั้งไฟล์
แปลงครั้งเดียวตอนเขียนกลับเท่านั้น · **นับ CR เดี่ยวหลังแก้ไฟล์ทุกครั้ง ต้องได้ 0 ทุกไฟล์**

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import glob
CR=13; LF=10
for f in sorted(glob.glob('*.html'))+['CLAUDE.md']:
    b=open(f,'rb').read()
    lone=sum(1 for i,c in enumerate(b) if c==CR and b[i+1:i+2]!=bytes([LF]))
    print(f, 'CR เดี่ยว =', lone, '' if lone==0 else '*** ต้องล้าง')
EOF
```

ถ้าเจอ ล้างด้วยการแทน `CR CR LF` ทั้งหมดเป็น `CR LF` แล้วเช็คซ้ำ

ถึงอย่างนั้นก็ยังควรตรวจ `git diff --stat` ก่อน commit ทุกครั้ง (ทางแก้ถาวรคือใส่ `*.html text eol=lf` แล้ว renormalize รอบเดียว — **ยังไม่ได้ทำ**)
**v2.6 ผู้สั่งงานสั่งลบ `products.html` กับ `warranty.html` ออกจากเว็บ — เหลือ 6 หน้า** (`ls *.html` · `index.html` คือ `/`)
**ไม่มีไฟล์ untracked เหลือแล้ว** — commit `898313b` (v2.7) เก็บงานตั้งแต่ v1.8 ถึง v2.7 ลง git ครบในก้อนเดียว รวม `gallery.html` · `data/dealers.json` · `README.md` ของทุกโฟลเดอร์ ที่เคยไม่มีจุดกู้คืนเลยสิบกว่าเวอร์ชัน (`git ls-files` = 43 ไฟล์) · **v3.3 commit และ push ครบแล้ว — `main` ตรงกับ `origin/main`** · ก่อนหน้านั้น `origin/main` ค้างอยู่ที่ `f8aa93e` (v1.7) มาตั้งแต่ 8 ส.ค. **งาน v1.8 ถึง v3.2 ไม่เคยขึ้น GitHub เลย** และแม้แต่ `898313b` ที่ commit แล้วก็ยังไม่ถูก push · **บทเรียน: commit แล้วไม่ได้แปลว่ามีสำเนานอกเครื่อง ต้องดู `git status -sb` ว่าขึ้น `ahead` ไหม** · ยังถามผู้สั่งงานก่อน commit/push ทุกครั้งเหมือนเดิม
**IA ที่ใช้จริงจึงไม่ใช่ 3 แกนของข้อ 8 อีกแล้ว** — กลายเป็น เทคโนโลยี · ผลงาน · ตัวแทน

**`products.html` กับ `warranty.html` ที่ถูกลบ ยังกู้จาก git ได้** — **กู้ด้วย `git checkout 3843aa5 -- products.html warranty.html` ได้** — แต่ commit นั้นคือสถานะ v1.6 **การแก้ทั้งหมดตั้งแต่ v1.7 ถึง v2.3 (รวมการลบบล็อกแจ้งใน v2.3) ไม่ได้ commit ไว้ จึงหายไปกับการลบ** ถ้ากู้จะได้เวอร์ชันเก่ากว่าที่เห็นล่าสุด · เทียบแล้ว `3843aa5` กับ `898313b^` (= `f8aa93e`) ให้ไฟล์เดียวกันทั้งสองหน้า **สองหน้านี้ไม่ถูกแตะใน git เลยตั้งแต่ v1.6 จนถูกลบ** จะกู้จาก commit ไหนก็ได้เนื้อหาเท่ากัน

## อ่านก่อนเริ่มงานทุกครั้ง

| ไฟล์ | บทบาท |
|---|---|
| `docs/master-prompt.md` | บรีฟหลัก v1.0 — **เป็นข้อผูกมัด** งานทุกชิ้นถูกตัดสินด้วยข้อที่มีหมายเลข (ข้อ 1–12) อ้างอิงเป็น "ข้อ N" เสมอ · ข้อ 2 คือ anti-brief ถ้าละเมิดคือรื้อทำใหม่ |
| `docs/CHANGELOG.md` | บันทึกการตัดสินใจ **ปัจจุบันถึง v3.0 และตรงกับไฟล์จริง** · เพิ่มหัวข้อใหม่ไว้**บนสุด** · ท้ายไฟล์มี "ค้างอยู่ 21 ข้อ" (ข้อ 3 · 5 · 6 · 7 · 9 · 16 ขีดฆ่าปิดแล้วแต่ยังนับอยู่ในรายการ) กับ "ตัวแปรที่ต้องขอ" ที่ต้องอัปเดตทุกครั้ง — **อ่านสองหัวข้อนี้ก่อนเริ่มงานเสมอ อย่าลอกรายการค้างมาไว้ที่นี่** |
| `docs/film-data.md` | **ค่าสเปกจริงทั้ง 15 รหัสฟิล์ม** ถอดจากโบรชัวร์ใน `scans/` · อธิบายโครงข้อมูล ข้อค้นพบที่ใช้เป็นเนื้อหาได้ และ 9 ข้อที่ต้องยืนยันกับลูกค้า — อ่านก่อนแตะตัวเลขใดๆ บนหน้าเว็บ |
| `data/films.json` | ข้อมูลชุดเดียวกันในรูปแบบที่เครื่องอ่านได้ — **เป็น source of truth ของตัวเลขทุกตัวบนเว็บ** · **v2.6 ลบหน้าที่แสดงค่ารายรหัสทิ้งแล้ว ข้อมูลชุดนี้จึงเหลือทางออกสู่หน้าเว็บแค่ตารางช่วงค่าใน `/about` กับรหัสฟิล์มบนการ์ดใน `/gallery`** — ตัวไฟล์ยังครบและตรวจย้อนได้เหมือนเดิม |
| `data/dealers.json` | **source of truth ของรายชื่อตัวแทนบน `/contact`** (**9 ราย** · ทุกรายยัง `placeholder:true` = ข้อมูลสมมติ · ค้างข้อ 18 — **CHANGELOG ข้อ 18 ยังเขียน 10 ราย ซึ่งเป็นตัวเลขก่อน v2.6 ตัด `building` ออก**) · **อยู่ใน commit `898313b` แล้วและ working tree สะอาด** — อ่าน `_readme` ในไฟล์ก่อนแตะ |
| `docs/review.html` | หน้าตรวจงานภายใน `noindex,nofollow` ไม่มีหน้าไหนลิงก์ถึง — **ห้ามขึ้นเซิร์ฟเวอร์จริง** · **เนื้อหาการตรวจค้างที่ v1.2 ตามหลัง 7 เวอร์ชัน** ป้ายเวอร์ชันในหน้ายังเขียน `1.2` · **ปัญหาข้อ 1 · 3 · 4 ในไฟล์นั้นปิดไปแล้วใน v1.6/v1.8 แต่หน้ายังแสดงว่าค้าง** และรายการตัวแปรยังเป็นชุด XT/CM/CL ที่เลิกใช้แล้ว **อย่าใช้ไฟล์นี้ตัดสินสถานะปัจจุบัน** · แต่**ผังไฟล์กับ path ข้างในอัปเดตแล้ว (v1.9 + เพิ่ม `gallery.html` และ `{{PORTFOLIO_ITEMS}}` ใน v2.1)** — **ผังเป็น 6 หน้าตรงกับไฟล์จริงแล้ว** (ไม่มี `products.html` / `warranty.html` เหลือในไฟล์ · วัดซ้ำ 26 ส.ค. 2569) และลิงก์ `logo-finnix.png` ที่เคยเสียถูกแทนด้วยข้อความธรรมดาแล้ว (ตรวจซ้ำ 20 ส.ค. 2569 — ไม่มีลิงก์เสียเหลือในไฟล์นี้) |

ดัชนีข้อของบรีฟ (ไว้เปิดตรงจุด ไม่ต้องอ่านทั้งไฟล์ 22 KB) — 0 บทบาท · **1 โจทย์ธุรกิจ** · **2 anti-brief** · 3 จุดยืนแบรนด์+น้ำเสียง · 4 design tokens (สี · ตัวอักษร · ระยะ · motion) · 5 งบ motion · 6 signature · 7 MODE A/B · **8 โครงสร้างเว็บ+IA** · 9 ข้อกำหนดเทคนิค · 10 กติกาข้อมูลที่ยังไม่มี · **11 เกณฑ์ตรวจรับ 9 ข้อ** · 12 รูปแบบการตอบ

## คำสั่ง

ไม่มี build / lint / test — ตรวจงานด้วยตาและด้วยเกณฑ์รับงาน 9 ข้อ (ข้อ 11) เท่านั้น

```powershell
python -m http.server 8000     # เสิร์ฟจาก root แล้วเปิด http://localhost:8000/
```

เปิดด้วย `file://` ก็ได้ แต่ควรใช้ local server เวลาทดสอบ motion ที่ผูกกับ scroll
ต้องตรวจสองความกว้างเสมอ — **1440px และ 375px** (จุดตัด 1080 / 900 / 620)
เป้า Lighthouse ≥ 95 ทั้งสี่หมวดตามข้อ 9 — **ยังไม่เคยวัดจริง**

## ของร่วม — แก้ที่ `tools/shared/` ที่เดียว แล้วกระจายด้วยสคริปต์ (v3.3)

ข้อ 9 บังคับว่าหนึ่งหน้าจบในไฟล์เดียว จึงแยก CSS/JS ออกไปเป็นไฟล์ร่วมไม่ได้ ของร่วมเลยถูกคัดลอกไว้ 6 ชุด
**v3.3 เก็บฉบับจริงไว้ที่ `tools/shared/` แล้ว ห้ามแก้เนื้อหาระหว่างเครื่องหมาย `@shared` / `@end` ในหน้าเว็บโดยตรงแล้วลืมดึงกลับ**

```bash
python tools/sync-shared.py                    # ตรวจอย่างเดียว ต้องได้ "ไม่ตรง 0 | ยังไม่คร่อม 0"
```

**คำสั่งที่เขียนไฟล์จงใจไม่ใส่ในบล็อกข้างบน** เพราะบล็อก ```bash ในไฟล์นี้ถูกดึงไปรันรวดเดียวเป็นชุดตรวจ — เคยเผลอรัน `--pull` ทับฉบับจริงมาแล้วใน v3.3 · พิมพ์เองทีละคำสั่งเท่านั้น
`python tools/sync-shared.py --write` กระจายฉบับจริงลงทั้ง 6 ไฟล์ · `python tools/sync-shared.py --pull index.html` ดึงจากไฟล์นั้นขึ้นเป็นฉบับจริง

**ลำดับที่ตั้งใจ** — แก้ของร่วมที่ `tools/shared/` (หรือแก้ในหน้าเว็บหน้าเดียวแล้ว `--pull` หน้านั้น) → `--write` → ตรวจให้ได้ `ไม่ตรง 0`

บริเวณที่ดูแลอยู่ **9 จุด** — `TOKENS` · `NAV_CSS` · `DRAWER_CSS` · `FOOTER_CSS` · `NOSCRIPT` · `NAV_HTML` · `DRAWER_HTML` · `FOOTER_HTML` · `JS_NAV`
`aria-current="page"` ใช้ token `@CUR:ชื่อหน้า@` ในฉบับจริง สคริปต์ใส่ให้ตามชื่อไฟล์ — **อย่าพิมพ์ `aria-current` ลงในฉบับจริงเอง**

**ข้อควรระวังที่เหยียบมาแล้ว** — ขอบเขตของสองบริเวณห้ามซ้อนกัน ตอนวางเครื่องหมายรอบแรก เครื่องหมายเปิดของ `DRAWER_CSS` ไปอยู่ในบริเวณของ `NAV_CSS` แล้วถูก `--write` เขียนทับหายทั้ง 6 ไฟล์
**ไฟล์ที่ขึ้นเซิร์ฟเวอร์ยังเป็นไฟล์เดียวจบต่อหนึ่งหน้าเหมือนเดิม ไม่ต้องรันอะไรก่อน deploy** — ลบ `tools/` ทิ้งเว็บก็ยังทำงานเหมือนเดิม รายละเอียดอยู่ที่ `tools/README.md`


### สคริปต์ตรวจ — เก็บไว้ที่นี่แทนการเขียนใหม่ทุกรอบ

**repo มีโฟลเดอร์ `tools/` แล้วตั้งแต่ v3.3** (ก่อนหน้านั้นไม่มีไฟล์สคริปต์เลยโดยตั้งใจ — v1.6 · v1.8 · v1.9 ต่างเขียนสคริปต์ขึ้นใช้ครั้งเดียวแล้วทิ้ง) · **`tools/` ไม่ใช่ build system** เว็บทำงานได้ครบโดยไม่ต้องรันอะไร ลบทิ้งก็ยังเหมือนเดิม · **สคริปต์ตรวจข้างล่างยังเก็บไว้ที่นี่เหมือนเดิม** — เพื่อไม่ให้เขียนใหม่อีก **ตัวที่ใช้บ่อยถูกย้ายมาไว้ในไฟล์นี้แล้ว คัดลอกไปวางได้เลย** ทั้งหมดรันใน Git Bash จาก root และ **ตรวจซ้ำแล้วว่าทำงานจริง 20 ส.ค. 2569**

เทียบตัวเลขบนหน้าแรกกับ `films.json` — ต้องได้ **`ตรวจ 5 ซีรีส์`** และ **`mismatches: 0`** พร้อมกันทั้งคู่ **รันทุกครั้งที่แตะการ์ดซีรีส์ใน `index.html`**

> **สคริปต์ตัวเดิมพังเงียบมาตั้งแต่ v3.0 จนถึง v3.2** — มันค้นหา `<b>CODE</b>` กับ `<i style="width:N%">` ซึ่งเป็น markup ของบล็อก `#series` ที่ถูกลบไปตั้งแต่ v1.6 · v3.0 เขียนการ์ดซีรีส์ใหม่เป็น `.cat-item` แต่ไม่มีใครแก้สคริปต์ตาม **มันจึงจับได้ 0 รายการ แล้วพิมพ์ `mismatches: 0` ออกมาเป็นการผ่านแบบหลอกทุกครั้ง**
> **บทเรียน: สคริปต์ตรวจต้องพิมพ์จำนวนที่จับได้ออกมาด้วยเสมอ ไม่ใช่พิมพ์แค่จำนวนที่ผิด** ไม่งั้นแยกไม่ออกระหว่าง "ตรวจแล้วผ่าน" กับ "ไม่ได้ตรวจอะไรเลย"

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import json,io,re
d=json.load(io.open('data/films.json',encoding='utf-8'))
mx={s['name']['en']:max(v['heatReject'] for v in s['variants']) for s in d['series']}
val={v['code']:v for s in d['series'] for v in s['variants']}

h=io.open('index.html',encoding='utf-8').read(); bad=0
rows=re.findall(r'<div class="nm">([A-Z ]+)</div><div class="v">(\d+)%</div>.*?<i style="width:(\d+)%"',h,re.S)
for name,pct,bar in rows:
    if int(pct)!=mx[name] or int(bar)!=mx[name]:
        print('MISMATCH',name,'ตัวเลข',pct,'แถบ',bar,'ควรเป็น',mx[name]); bad+=1
print('index: ตรวจ',len(rows),'ซีรีส์ (ต้องเป็น 5) | mismatches:',bad)

g=io.open('gallery.html',encoding='utf-8').read(); bad2=0
cards=re.findall(r'<div class="ph" style="--vlt:(\d+)">.*?บานข้าง <b>([A-Z]{2}\d{2})</b>',g,re.S)
for vlt,code in cards:
    if int(vlt)!=val[code]['vlt']:
        print('MISMATCH',code,'--vlt',vlt,'ควรเป็น',val[code]['vlt']); bad2+=1
print('gallery: ตรวจ',len(cards),'การ์ด (ต้องเป็น 20) | mismatches:',bad2)
EOF
```

ตัวนี้ตรวจสองที่พร้อมกัน — **ตัวเลขกับความกว้างแถบใน `.cat-item` ของหน้าแรก** และ **ค่า `--vlt` ของกระจกจำลองบนการ์ดใน `/gallery`** ซึ่งต้องตรงกับรหัสฟิล์มบานข้างของการ์ดใบนั้น
ทดสอบแล้วว่าจับผิดได้จริง (แก้ค่าให้ผิดหนึ่งจุดแล้วมันขึ้น `MISMATCH` ทั้งสองฝั่ง)

เทียบค่าช่วงระดับซีรีส์ใน `/about` กับ `films.json` — ต้องได้ `about mismatches: 0` **รันทุกครั้งที่แตะตัวเลือกซีรีส์ใน `about.html`** (เพิ่มใน v2.9 · ก่อนหน้านั้นต้องคำนวณด้วยมือ)

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import json,io,re
d=json.load(io.open('data/films.json',encoding='utf-8'))
h=io.open('about.html',encoding='utf-8').read(); bad=0
for s in d['series']:
    code=s['codePrefix']; v=s['variants']
    i=h.index('id="pn-'+code+'"'); pan=h[i:h.index('</dl>',i)]
    for label,key in [('ลดความร้อน','heatReject'),('ป้องกันยูวี','uv'),('แสงส่องผ่าน','vlt')]:
        xs=[x[key] for x in v if x[key] is not None]; lo,hi=min(xs),max(xs)
        want=(str(lo)+'–'+str(hi)+'%') if lo!=hi else (str(hi)+'%')
        m=re.search(re.escape(label)+r'</dt><dd><b>([^<]+)</b>',pan)
        if not m or m.group(1)!=want:
            print('MISMATCH',code,label,m.group(1) if m else None,want); bad+=1
print('about mismatches:',bad)
EOF
```

เทียบรายชื่อตัวแทนใน `contact.html` กับ `dealers.json` — ต้องได้ `id ตรงกัน: True` และ `ต่างกัน: ไม่มี` **รันทุกครั้งที่แตะรายชื่อตัวแทน**

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import json,io,re
d=json.load(io.open('data/dealers.json',encoding='utf-8'))['dealers']
h=io.open('contact.html',encoding='utf-8').read()
e=json.loads(re.search(r'<script id="dealers-data"[^>]*>(.*?)</script>',h,re.S).group(1))
ids=[x['id'] for x in d]; eids=[x['id'] for x in e]
print('json:',len(d),'| embedded:',len(e),'| id ตรงกัน:',ids==eids)
print('ต่างกัน:',[k for k in range(min(len(d),len(e))) if d[k]!=e[k]] or 'ไม่มี')
print('placeholder ที่ยังเป็น true:',sum(1 for x in e if x.get('placeholder')))
EOF
```

`placeholder ที่ยังเป็น true` ต้องเป็น **0 ก่อนขึ้นเซิร์ฟเวอร์จริง** — ตอนนี้เป็น 9 จาก 9

ตรวจของร่วมทั้ง 6 ไฟล์ — บรรทัดแรกต้องขึ้น `6` เสมอ ถ้าไม่ใช่แปลว่าแก้ `:root` ไม่ครบทุกไฟล์

```bash
for f in *.html; do sed -n '/^:root{/,/^}/p' "$f" | tr -d '\r' | md5sum | sed "s|-|$f|"; done | sort | uniq -c -w32
grep -c 'btn-red' *.html                        # ต้องเป็น 0 ทุกไฟล์รวม technology — ค้างข้อ 7 ปิดแล้วใน v2.8
grep -n 'type="range"' *.html                   # ต้องเจอที่ technology ที่เดียว
git ls-files --eol | grep '\.html'              # ดู i/ กับ w/ ก่อน commit
echo "distinct=$(grep -oh '{{[A-Z_]*}}' *.html|sort -u|wc -l) total=$(grep -oh '{{[A-Z_]*}}' *.html|wc -l)"   # ต้องได้ 26 / 34 (วัดซ้ำ 26 ส.ค. 2569)
```

หา CSS ตาย (นิยาม: มี selector แต่ไม่มี `class=` ไหนเรียก)

**สคริปต์เดิมที่ไล่ทีละชื่อคลาสถูกถอดทิ้งใน v2.8 เพราะให้คำตอบผิด** — มันเทียบชื่อคลาสด้วยขอบคำ ซึ่งนับ `-` เป็นขอบคำด้วย ทำให้ `class="eyebrow on-dark"` ถูกนับเป็นการเรียก `.dark` · ตัวใหม่ไล่ทุกคลาสในไฟล์เอง และกัน false positive ของคลาสที่ JS สร้างด้วย `className`

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import io,re,glob
for f in sorted(glob.glob('*.html')):
    s=io.open(f,encoding='utf-8').read()
    s=re.sub(r'<noscript>.*?</noscript>','',s,flags=re.S)  # noscript เป็นชั้น override ไม่ใช่ CSS ของหน้า ห้ามถอดบรรทัดนี้
    css=''.join(re.findall(r'<style>(.*?)</style>',s,re.S))
    css=re.sub(r'/\*.*?\*/','',css,flags=re.S)          # ตัดคอมเมนต์ CSS ทิ้งก่อน ห้ามถอดบรรทัดนี้
    js=''.join(re.findall(r'<script>(.*?)</script>',s,re.S))
    rest=re.sub(r'<script>.*?</script>','',re.sub(r'<style>.*?</style>','',s,flags=re.S),flags=re.S)
    defined=set(re.findall(r'[.]([a-zA-Z][\w-]*)(?=[\s,{:.\[>+~)])',css))
    used=set()
    for cl in re.findall(r'class="([^"]*)"',rest): used.update(cl.split())
    dead=sorted(c for c in defined if c not in used and c not in js)
    print(f"{f}: {' '.join(dead) if dead else 'ไม่มี'}")
EOF
```

**ผลปัจจุบัน — ไม่มี CSS ตายเหลือทั้ง 6 ไฟล์ และไม่มี false positive เหลือด้วย** (26 ส.ค. 2569 ต้องขึ้น `ไม่มี` ครบทุกบรรทัด)
**จุดบอด: สคริปต์นี้ไล่แต่ชื่อคลาส ไม่ได้ไล่ selector ที่เป็นชื่อ element** — v3.1 เจอ `.form-ok h3` ที่กำพร้ามาตั้งแต่ v3.0 (markup เปลี่ยนเป็น `h2` ตอนแก้ลำดับหัวข้อ แต่ CSS ไม่ได้ตามมา หัวข้อจึงหลุดสไตล์ไปใช้ค่า default ของเบราว์เซอร์) **สคริปต์ขึ้น `ไม่มี` ทั้งที่มีของกำพร้าอยู่จริง** · ถ้าแก้ลำดับหัวข้อหรือเปลี่ยนชื่อ element เมื่อไหร่ ต้องไล่ selector แบบ `.class hN` ด้วยมือเพิ่ม
บรรทัดตัดคอมเมนต์ CSS เพิ่งเพิ่มเข้ามา 26 ส.ค. 2569 — ก่อนหน้านั้นชื่อคลาสที่ถูกเอ่ยถึงใน**คอมเมนต์** ถูกนับเป็น "นิยามไว้" ทำให้ `index.html` รายงาน `com filtered handle json scene-tag split-line why-cell` ทั้งที่สี่ตัวกลางถูกลบไปแล้วจริงใน v3.0 · **ถ้าเห็นชื่อพวกนี้โผล่มาอีก แปลว่ามีคนถอดบรรทัดนั้นออก ไม่ใช่ CSS ตายกลับมา**
ที่ลบไปใน v2.8 — `.disp` (อยู่ในกลุ่ม `h1,h2,h3,h4,.disp` — **ลบเฉพาะตัวมันเอง ไม่ใช่ทั้ง rule ไม่งั้นฟอนต์หัวเรื่องหายทั้งเว็บ**) · `.smoke` ที่ `faq` · `.stand` ที่ `index` (เศษจาก section จุดยืนที่ถูกแทนใน v2.7) · `.spread` กับ `.dark` และลูกอีก 3 rule ที่ `technology`

## กฎที่ละเมิดแล้วงานไม่ผ่าน

- **ไฟล์เดียวจบต่อหนึ่งหน้า** — request ภายนอกมีได้แค่ Google Fonts **บวกแผนที่ Google ที่ `contact.html` ซึ่งเป็นข้อยกเว้นที่ผู้สั่งงานอนุมัติเองใน v2.5** · **ห้ามดึงไลบรารี JS/CSS ภายนอกอยู่เหมือนเดิม** และห้ามขยายข้อยกเว้นนี้ไปหน้าอื่นโดยไม่ถาม (เหตุผลที่ signature เป็น SVG ไม่ใช่ WebGL อยู่ใน CHANGELOG v1.1)
- **ฟอนต์ Anuphan (หัวเรื่อง) + IBM Plex Sans Thai (เนื้อหา) เท่านั้น** ฟอนต์ที่ไม่มีชุดภาษาไทยห้ามใช้เด็ดขาด
- **`<main id="main">` + skip link ต้องมีทุกหน้า** ครบทั้ง 6 แล้ว (v1.9 + `gallery.html` ใน v2.1) — หน้าใหม่ต้องมีด้วย
- **Motion ไม่เกิน 4 อย่างต่อหน้า** และต้องเคารพ `prefers-reduced-motion` (JS ทุกไฟล์แตกสาขาด้วยตัวแปร `reduce`) — **`index.html` เหลือ 3 อย่างตั้งแต่ v3.0** (text reveal · scroll reveal `.rv` · ตัวนับ `.cnt`) หลังถอดตัวเลื่อนเทียบใน hero ออก **จึงเหลือโควตาอีก 1 เป็นครั้งแรกตั้งแต่ v1.0** · **`technology.html` เต็ม 4/4 แล้ว เพิ่มอะไรไม่ได้อีก** (คอมเมนต์ `MOTION 1`–`MOTION 4` ในไฟล์คือรายการจริง) · อีก 4 หน้า (`about` `contact` `faq` `gallery`) มี 2 อย่าง เหลืองบอีก 2
- **สีแดงไม่เกิน 2 จุดต่อหนึ่งหน้าจอ** ใช้ได้เฉพาะปุ่มหลัก · จุด eyebrow · ชั้นเซรามิกในไดอะแกรม · focus/error
  **แสงเรืองพื้นหลัง (radial-gradient สีแดง) ไม่อยู่ในรายการนี้** — v3.1 ถอดออกจาก `contact` สองจุด (`.closing::before` กับ `.page-head::before`) ที่ทำให้หน้าจอนั้นมีแดง 3 จุด **ห้ามใส่กลับ**
  **ปิดแล้วใน v3.3** — เส้นใต้ลิงก์หน้าปัจจุบันเคยเป็นแดงค้างใน 5 หน้า (แดง 3 จุดบนหัวจอทุกหน้า) · `technology.html` ทำถูกมาตั้งแต่ commit แรกด้วย `background:currentColor;opacity:.28` เพราะเป็นหน้าแรกสุดที่เขียน ส่วนอีก 5 หน้าคัดลอกไปแล้วทำเป็นแดง · **ยกเวอร์ชันของ technology ขึ้นเป็นฉบับจริงใน `tools/shared/NAV_CSS.txt` แล้ว วัดในเบราว์เซอร์ได้แดง 2 จุดตามโควตา** · จะเอาเส้นแดงกลับ แก้บรรทัดเดียวที่ฉบับจริงแล้ว `--write`
- **สีข้อความต้องผ่าน WCAG AA 4.5:1 — วัดก่อนเปลี่ยนทุกครั้ง** · v3.0 แก้ `--mute` เป็น `#70706A` (เดิม `#75756F` ได้แค่ 4.21:1 บนพื้น `--smoke`) และ `.f-col h3` เป็น alpha `.46` (เดิม `.42` = 3.82:1) · **`--mute-dark` `.52` = 5.67:1 ผ่านอยู่แล้ว** · **บทเรียนจาก v2.8: การทำ CSS ให้ "เข้าชุด" ตามเสียงข้างมากโดยไม่วัด contrast ทำให้ค่าที่เคยผ่านกลายเป็นไม่ผ่าน** — เข้าชุดกับผ่าน AA ต้องได้ทั้งคู่
- **ทุกระยะหารด้วย 8 ลงตัว**
- **ห้ามแต่งตัวเลขสเปก** ค่าที่ยังไม่มีต้องเขียนเป็น `{{VAR}}` และต้องไปเพิ่มในรายการตัวแปรทั้งใน CHANGELOG และ `docs/review.html`
- **มีค่าจริงแล้วก็ยังขึ้นไม่ได้ จนกว่าจะได้ `{{TEST_STANDARD}}`** ข้อ 3 บังคับว่าทุกตัวเลขต้องบอกได้ว่าวัดด้วยมาตรฐานใด โบรชัวร์เขียนแค่ "ตามมาตรฐานสากลของโลก" — **นี่คือตัวปลดล็อกตัวเดียวที่กั้นตัวเลขทั้งชุดอยู่** · **v2.3 ลบบล็อกแจ้งบนหน้าออกตามคำสั่งผู้สั่งงาน — หน้าเว็บจึงไม่เตือนอะไรอีกแล้ว แต่ข้อห้ามยังอยู่เท่าเดิม** (ค้างข้อ 1)
- **ห้ามใช้ localStorage / sessionStorage**
- **Signature มีได้ชิ้นเดียวทั้งเว็บ** = ภาพตัดขวางชั้นฟิล์มในส่วนที่ 3 ของ `technology.html` · ตัวซ้ำที่ `#tech` ในหน้าแรกถอดออกแล้วใน v1.8 **ห้ามใส่กลับ** · แผนที่ SVG ที่วาดเองใน v2.4 ถูกแทนด้วยแผนที่ Google จริงแล้วใน v2.5 ประเด็นนี้จึงปิดไป
- **CTA เดียวต่อหน้า** ปลายทางเดียวของทั้งเว็บคือ `https://finnixcenterth.com` · **ปุ่มแดงมีได้ปุ่มเดียวต่อหน้าจอ และ nav CTA กินโควตานั้นไปแล้ว** ทุกหน้าจึงใช้ `.btn-light` (ขาวบนพื้นมืด) ในส่วนปิดท้าย ห้ามเปลี่ยนกลับเป็นแดง — **ครบทั้ง 6 หน้าแล้วตั้งแต่ v2.8** (`grep -c btn-red *.html` ต้องเป็น 0 ทุกไฟล์ · `.btn-red` ถูกลบออกจาก `technology.html` พร้อมกัน)
  **ทุกหน้ามีปุ่มปิดท้ายปุ่มเดียวจริงแล้วตั้งแต่ v2.3** (ปุ่มที่สองใน `contact` ถูกตัด) · แต่ `contact.html` ปิดท้ายชี้ `#dealers` ในหน้าตัวเอง ไม่ใช่ `finnixcenterth.com` เหมือนอีก 5 หน้า **อันนี้ตั้งใจ — ปลายทางที่ใกล้กว่าคือตัวแทนในจังหวัดผู้อ่าน**
- **ห้ามคัดลอก layout ของ 3M / Solar Gard** (ข้อ 2.9) — แต่ข้อ 1 อนุญาตให้ใช้เป็น**โครงสร้าง IA** ได้ (บันทึกใน CHANGELOG v1.5) · **v2.2 ผู้สั่งงานยกเส้นนี้ขึ้นเองสำหรับ `contact` กับ `gallery`** — ตามโครงหน้าต้นแบบทั้งลำดับและองค์ประกอบ แต่ render ด้วย token/ฟอนต์/กฎของเราทั้งหมด เป็นการต่างจากบรีฟโดยตั้งใจแบบเดียวกับปม 5 ซีรีส์ v1.6
- ทุกหน้าทำในโหมด **MODE B** (ไม่มีงบภาพ) — ไม่มีภาพถ่ายเลย มีแต่โลโก้ ที่เหลือเป็น SVG และ typography · ห้ามใช้ภาพ stock

## โครงสร้างโค้ดที่ใช้ซ้ำทุกหน้า

ทั้ง 6 ไฟล์ใช้โครงเดียวกันและ **คัดลอกกันมาทั้งบล็อก** — nav, drawer, footer, `:root` tokens, `.rv`, `.var`, ปุ่ม `.btn`, **บล็อก SEO meta**, **บล็อก `<noscript>` (v3.1)**
**บล็อก `<noscript>` ต้องเหมือนกันเป๊ะทั้ง 6 ไฟล์** — อยู่ใต้ `</style>` คืน `opacity/transform` ให้ `.rv` `.rvl` และ hero เผื่อ JS ไม่ทำงาน · selector ที่หน้านั้นไม่มีก็ไม่มีผล **แต่ถ้าเขียนต่างกันรายไฟล์ สคริปต์ตรวจของร่วมจะอ่านเป็น `.rv` ที่ไม่ตรงกันทันที** (สคริปต์รวมทุก `<style>` ในไฟล์ รวมตัวใน `noscript` ด้วย)
**`[id]{scroll-margin-top:96px}` (v3.1)** อยู่ใต้ `html{}` ทุกไฟล์ — nav ลอยสูง 76px ถ้าไม่มีบรรทัดนี้ ทุก anchor รวมทั้ง `#main` ของ skip link จะไปมุดใต้ nav
`:root` เหมือนกันทุกบรรทัดทุกไฟล์ (ตรวจแล้ว hash ตรงกันทั้ง 6) **แก้ที่ใดต้องแก้ครบทั้ง 6 ไฟล์พร้อมกันเสมอ** ไม่งั้น nav จะขยับไม่เท่ากันเวลาสลับหน้า
ต้นทุนของการซ้ำสูงขึ้นเรื่อยๆ (จาก 2 เป็น 6 จุดต่อการแก้หนึ่งครั้ง) แต่ข้อ 9 บังคับว่าหนึ่งหน้าต้องจบในไฟล์เดียว จึงยังแยกไฟล์ร่วมไม่ได้
**v3.3 แก้ที่ต้นทุนการดูแลแทน — ของร่วม 9 บริเวณมีฉบับจริงที่ `tools/shared/` แล้ว แก้จุดเดียวกระจายครบ 6 ไฟล์ด้วย `python tools/sync-shared.py --write`** · ขนาดไฟล์ยังเท่าเดิมเพราะข้อ 9 บังคับให้ทุกหน้าพกสำเนาของตัวเอง (วัดแล้ว: แยกเป็นไฟล์ CSS ร่วมจริงจะประหยัด ~31 KB = 11% แต่ชนข้อ 9 ต้องให้ผู้สั่งงานยกเส้นเอง) · **footer markup แฮชตรงกันทั้ง 6 ไฟล์ · nav ต่างกันเฉพาะ `aria-current="page"` ของหน้าตัวเอง (ตรวจซ้ำ 25 ส.ค. 2569 — แฮช nav จึงไม่ตรงกันโดยตั้งใจ อย่าตกใจ) แต่ CSS ไม่ตรง**

**บล็อก SEO meta เป็นของร่วมชิ้นที่เพิ่มใน v3.0** — `og:type` · `og:site_name` · `og:locale` · `og:title` · `og:description` · `twitter:card` มีครบทั้ง 6 หน้า (สี่ตัวที่ไม่ผูกกับเนื้อหาหน้าแฮชตรงกันทั้ง 6 · ตรวจ 26 ส.ค. 2569) · เหนือบล็อกมีคอมเมนต์บอกไว้ว่า **`og:url` · `canonical` · `og:image` จงใจไม่ใส่** เพราะโดเมนยังไม่นิ่ง (ค้างข้อ 15) และ MODE B ไม่มีภาพ 1200x630 — **อย่าไป "เติมให้ครบ" โดยไม่อ่านคอมเมนต์นั้นก่อน**
**CSS ซ้ำระหว่าง 6 ไฟล์ 37%** (~33,000 จาก 88,684 ตัวอักษร) วัดไว้แล้วใน v3.0 · **ไม่ต้องวัดซ้ำ** (ค้างข้อ 21) — เป็นหนี้ที่ข้อ 9 บังคับให้ยอมรับตราบใดที่หนึ่งหน้าต้องจบในไฟล์เดียว

**ทุกไฟล์มีสารบัญ CSS อยู่หัวบล็อก `<style>` ตั้งแต่ v3.2** — บอกลำดับบล็อกในไฟล์ · รายชื่อของร่วมที่ต้องแก้ครบ 6 ไฟล์ · จุดตัด responsive · เอกสารที่ต้องอ่านก่อนแก้
**จงใจไม่ใส่เลขบรรทัด** เพราะมันเลื่อนทุกรอบที่แก้ไฟล์ (กติกาเดียวกับที่ห้ามหา `type="range"` ด้วยเลขบรรทัด) · **เพิ่มบล็อกใหม่ใน `<style>` เมื่อไหร่ ให้เติมชื่อลงสารบัญด้วย**
**ห้ามเขียนชื่อแท็ก html เป็นตัวอักษรในคอมเมนต์นี้** — v3.2 เคยเขียนคำว่า noscript แบบมีวงเล็บมุมไว้ในสารบัญ แล้วสคริปต์ที่ค้นด้วย regex จับช่วงผิดยาวทั้งไฟล์ (ตัว HTML ไม่พัง เพราะข้างใน `<style>` เป็น raw text)

**`technology.html` คือไฟล์ที่หลุดจากชุดอย่างเป็นระบบ ไม่ใช่ความต่างกระจัดกระจาย** — เป็นหน้าแรกสุดที่เขียน อีก 5 หน้าถูกทำให้ตรงกันทีหลังโดยไม่ได้ย้อนกลับมาแก้ไฟล์นี้ **จะแก้ของร่วมเมื่อไหร่ ให้เปิด `technology.html` เทียบเป็นไฟล์สุดท้ายเสมอ**

| จุด | สถานะ |
|---|---|
| **v3.0 ปิดเกือบครบ** | v2.8 แก้ตามตารางเดิมที่มี 5 รายการ แต่สคริปต์ไล่ selector จริงพบว่าต่างอยู่ **10 จุด** — `footer` · `.f-grid` · `.f-bot` · `.f-brand p` · `.f-col a` · `.f-col a:hover` · `.eyebrow .dot` · `.closing` · `.closing h2` · `.closing p` · **ตรงกันหมดยกเว้น `.f-grid`** |
| **`.f-grid` ยังต่างจริง** | `technology` มี `padding:56px 0 40px` เกินมาในบล็อก `@media(max-width:620px)` · **สคริปต์ตรวจข้างล่างมองไม่เห็น เพราะมันตัดบล็อก `@media` ทิ้งก่อนเทียบ** — ความต่างที่อยู่ในเบรกพอยต์จึงหลุดทุกครั้ง ซึ่งเป็นที่ที่ของร่วมอย่าง `footer`/`.f-grid`/`.drawer`/`.page-head` มีสาระอยู่จริง |
| ที่ยังต่างโดยตั้งใจ | `.closing` ของ `contact` (มี `position:relative` สำหรับ element ซ้อน) · `.closing h2` ของ `index` (clamp 76px) · `.page-head*` ที่ต่างตาม `max-width` ของข้อความและการจัดวางของแต่ละหน้า |

**วิธีตรวจว่ายังเข้าชุดอยู่ไหม — รันทุกครั้งที่แตะของร่วม**

```bash
export PYTHONIOENCODING=utf-8
python - <<'EOF'
import io,re,glob
rules={}
for f in sorted(glob.glob('*.html')):
    src=io.open(f,encoding='utf-8').read()
    src=re.sub(r'<noscript>.*?</noscript>','',src,flags=re.S)   # ห้ามถอด — ไม่งั้น .rv จะขึ้นว่าไม่ตรงกัน
    c=''.join(re.findall(r'<style>(.*?)</style>',src,re.S))
    c=re.sub(r'/\*.*?\*/','',c,flags=re.S)
    c=re.sub(r'@[a-z-]+[^{]*\{(?:[^{}]|\{[^{}]*\})*\}','',c,flags=re.S)
    for sel,body in re.findall(r'([^{}]+)\{([^{}]*)\}',c):
        rules.setdefault(' '.join(sel.split()),{})[f]=body.strip()
for sel,per in sorted(rules.items()):
    if len(per)>=4 and len(set(per.values()))>1:
        print(sel,'->',{f:len(b) for f,b in per.items()})
EOF
```

**v2.8 ทำ `.btn` กับ `.f-col h4` ให้ตรงกันครบทั้ง 6 ไฟล์แล้ว** (`margin-bottom` เดิม 20px ที่ `index`/`technology` → 16px ตามอีก 4 หน้า · หัวคอลัมน์ footer จึงไม่ขยับเวลาสลับหน้าอีก) · **ที่ยังเหลือ 4 selector ที่ต่างโดยตั้งใจ** — `.closing h2` clamp 76px ที่ `index` กับ 72px ที่อีก 5 หน้า · `.page-head` · `.page-head h1` · `.page-head p` (ต่างตามความยาวข้อความและการจัดวางของแต่ละหน้า) · **บวก `.f-grid` ที่สคริปต์มองไม่เห็นเพราะต่างกันในบล็อก `@media`** — รายการเต็มอยู่ที่ค้างข้อ 6 ของ CHANGELOG
**`.btn{padding:0 30px}` ที่ทำให้ตรงกันนั้นหารด้วย 8 ไม่ลงตัว** — v2.8 เลือกตามเสียงข้างมาก 5 ไฟล์เพื่อปิดค้างข้อ 6 ซึ่ง**สวนกับเกณฑ์ข้อ 5** · `technology` เดิมเป็น `0 32px` ที่หารลงตัว · **ถ้าจะปิดค้างข้อ 8 ให้เปลี่ยนทั้ง 6 ไฟล์เป็น 32px รอบเดียวพร้อม `gap:30px` · `margin-right:10px` · burger `17px`**
`.drawer` ต่างกันเล็กน้อยตรงสถานะลิงก์ปัจจุบัน **อันนี้ตั้งใจ** · `:root` ตรงกันทุกบรรทัดทั้ง 6 ไฟล์จริง (ยืนยันซ้ำ 25 ส.ค. 2569)

ลำดับ section — nav ลอย → hero พื้น `--void` → สลับพื้น `--paper` / `--smoke` → section ปิดท้ายพื้นมืด + CTA → footer
5 หน้าที่ไม่ใช่หน้าแรกใช้ `.page-head` (hero แบบสั้น padding 200px) แทน hero เต็มจอ และ **ไม่มี signature เลย** ตามข้อ 6 ที่ห้ามสร้างของว้าวชิ้นที่สอง

nav มี **5 ลิงก์ตั้งแต่ v2.6** (หน้าแรก · เกี่ยวกับเรา · เทคโนโลยี · ผลงาน · ติดต่อ) + CTA แดง · FAQ อยู่ใน footer และ drawer
**จุดพับเมนูยังเป็น 1080px** ที่ย้ายมาตอน nav มี 7 ลิงก์ (v2.2) · พอเหลือ 5 ลิงก์ **น่าจะย้ายกลับ 900 ได้แล้วแต่ยังไม่ได้วัด** — ไม่ได้ทำเพราะ 1080 ไม่ทำให้อะไรพัง (ค้างใน CHANGELOG v2.6)
**footer เหลือ 3 คอลัมน์** (แบรนด์ · บริษัท · บริการ) — คอลัมน์ "ผลิตภัณฑ์" หายไปพร้อมหน้าที่มันชี้ทั้ง 5 ลิงก์
**ลิงก์ทุกจุดชี้ไฟล์จริงหมดแล้ว ไม่มีลิงก์ตายเหลือในเว็บ** — v1.8 เอา `href="#"` ของปุ่ม "ดูผลงานทั้งหมด" ออกพร้อม `#work` **แต่ยังเหลืออีกตัวที่ `contact` (`#form-reset`) จนถึง v3.1 ที่เปลี่ยนเป็น `<button type="button">` — ประโยคนี้เพิ่งเป็นจริงใน v3.1 ไม่ใช่ v1.8**

**หน้าแรกรื้อตาม `spms-est.com` ใน v3.0** — hero จัดกลาง → ตัวเลขบริษัท `.figs` → **แถบจุดขาย 4 ช่อง `.usp`** → **หมวดสินค้าพื้นมืด + ชิปเลือก `.cats`** → ที่มาของบริษัท → **แถบค้นหาตัวแทน `.finder`** → **FAQ ย่อ `.faq-lite`** → ปิดท้าย
**`spms-est.com` เป็นเว็บผู้จัดจำหน่าย 3M** จึงชนข้อ 2.9 ตรงตัว · ผู้สั่งงานสั่งเอง เส้นแบ่งเดิมยังใช้: เอาลำดับและวิธีจัดแถว-คอลัมน์มา **ห้ามลอกกริดหรือ component** · สี่อย่างที่เขามีแต่เราทำไม่ได้และ**ตัดสินไปแล้ว อย่ารื้อซ้ำ** — hero ภาพรถเต็มจอกับภาพทุกชนิด (MODE B) · หัวข้อแดงตัวใหญ่ทุก section กับไอคอน 4 วงกลมแดง (ข้อ 4) · รีวิวผลงาน/ข่าว/Blog/ผลิตภัณฑ์ดูแลรถ/สมัครตัวแทน (ไม่มีข้อมูลและไม่มีหน้าปลายทาง) · ฟิล์มเปลี่ยนสีรถกับเคลือบเซรามิก (ไม่มีสินค้า)

**`#scene` ตัวเลื่อนเทียบก่อน-หลังถูกถอดออกทั้งชุดใน v3.0** พร้อม CSS `.filtered .split-line .handle .scene-tag .sweeping` และ JS ทั้งบล็อก — ผู้สั่งงานเลือกเองโดยรู้ว่าเสีย motion 1 ใน 4 และเสีย hero เต็มจอหน้าเดียวของเว็บ **ห้ามใส่กลับโดยไม่ถาม** · พื้นหลัง hero ยังเป็น `.glare`+`.grain` ที่เป็น gradient ล้วน ไม่ใช่ภาพ
~~บล็อกจุดขาย 4 บล็อก (v2.7)~~ ~~`.axes` สามช่อง~~ **ถูกแทนที่ทั้งหมดใน v3.0** — สาระย้ายไปอยู่ใน `.usp` กับการ์ดซีรีส์ใน `.cats` ซึ่งยังมาจาก `films.json` ทั้งหมด
**ตัวเลขทุกตัวบนหน้าแรก generate จาก `data/films.json` ตอนสร้าง markup ไม่ได้พิมพ์มือ** — ถ้าจะแก้ ให้ generate ใหม่อย่าแก้ทีละตัว · **คำถามใน `.faq-lite` ดึงจาก `faq.html` ตรงๆ** ทั้งคำถามและย่อหน้าแรกของคำตอบ ถ้าแก้ที่ `/faq` ต้องมาแก้ที่นี่ด้วย
**ลำดับพื้นหลังคือ void → paper → smoke → ink → paper → smoke → paper → ink** · `.usp` เป็น smoke การ์ดขาว ถ้าเปลี่ยนเป็น paper จะติดกับ `.figs` สองบล็อกรวด
**ไอคอนใน `.usp-cell` เป็น `--ink` ไม่ใช่แดงแบบหน้าต้นแบบ** — สี่ช่องอยู่ในหน้าจอเดียวกันเสมอ (บทเรียนเดียวกับ `.why-cell` ที่ `/about`)

**เส้นแบ่งเนื้อหาระหว่างหน้าแรกกับ `/about` — ห้ามให้ซ้ำกันอีก**
ตัวเลขระดับองค์กร (`{{YEAR_FOUNDED}}` · 3M+ ตารางฟุต · 7 ปี · `{{SERVICE_CENTERS}}`) กับเรื่องที่มาของการก่อตั้ง **อยู่หน้าแรกที่เดียว**
เรื่องผู้ผลิต (`{{FILM_SOURCE}}`) กับใบรับรอง (`{{CERTIFICATIONS}}`) **อยู่ `/about` ที่เดียว** · แต่ละหน้ามีลิงก์ชี้ไปอีกหน้า
v1.8 เคยมีบล็อกตัวเลขทั้งสองหน้าซ้ำกัน 3 ใน 4 ช่องโดยไม่รู้ตัว **ก่อนเพิ่มบล็อกตัวเลขที่ไหนก็ตาม ให้เช็คอีกหน้าก่อน**

JS เป็น IIFE ตัวเดียวท้ายไฟล์ ไม่มี dependency แพตเทิร์นที่ใช้ซ้ำ
**ลำดับสำคัญ: บล็อก reveal ต้องอยู่บนสุดถัดจาก `var reduce` (v3.1)** — ไม่มี try/catch สักไฟล์ ถ้าโค้ดเฉพาะหน้าที่อยู่ก่อนหน้า throw ขึ้นมา `.rv` จะค้างที่ `opacity:0` ทั้งหน้า (เดิมอยู่ท้ายสุด `contact` จึงเสี่ยงที่สุดเพราะมีโค้ดตัวค้นหาตัวแทน ~145 บรรทัดนำหน้า) · **ห่อด้วย `if(!reduce){…}` เท่านั้น ห้ามกลับไปใช้ `if(reduce) return;`** เพราะ early return ที่อยู่บนสุดจะฆ่า nav · drawer · แท็บ · ตัวค้นหาตัวแทน ของคนที่ปิด motion ทั้งหมด · **`technology.html` ไม่ย้ายโดยตั้งใจ** — reveal เป็น `MOTION 2` อยู่ก่อน `MOTION 3/4` อยู่แล้ว และการย้ายจะทำให้เลข MOTION สลับลำดับ
- `.loaded` บน body สั่ง text reveal ตอนเข้าหน้า (event `load` + `setTimeout` 400ms กันพลาด)
- `nav.solid` สลับเมื่อ `scrollY > 40` — ตัวนี้คือที่มาของสถานะโลโก้สองแบบ
- IntersectionObserver จับ `.rv` ทำ scroll reveal · stagger 60ms แบบ `(i % 4)` · `unobserve` ทันทีที่เข้า
- ไดอะแกรมชั้นฟิล์มผูกกับ scroll — คำนวณ progress จาก `getBoundingClientRect()` ของ section แล้ว `translateY` แต่ละ `.layer` พร้อมติด `.on` ให้รายการ legend · **เหลือที่ `technology.html` ไฟล์เดียว** (`#sig`/`#sig-legend`)
- เฉพาะหน้าแรก — ตัวนับ `.cnt[data-to]` (IntersectionObserver ตัวที่สอง threshold .6) · **ชิปเลือกหมวด `.chip` (v3.0)** `role=tablist` + roving tabindex + ลูกศรซ้ายขวา สลับ `hidden` ของ `.cat-panel` — **สถานะ UI ไม่นับเป็น motion** และอยู่ก่อน `if(reduce) return` โดยตั้งใจ · ~~ตัวเลื่อนเทียบ `#scene`~~ **ถอดออกแล้วใน v3.0**
- เฉพาะหน้าติดต่อ — ฟอร์ม `#cform` **ตอนนี้ปุ่มส่งเปิดอยู่และทำงานเป็น "โหมดสาธิต"** (validate ตอน blur · honeypot ช่อง `website` · `setTimeout` 600ms แล้วสลับ `.busy` → `.sent`) **ไม่มีปลายทางรับจริง** มีป้ายบอกบนหน้า 2 จุดและ `TODO` ในโค้ด · **ขัดกับข้อกำหนดเดิมที่ v2.2 ตั้งไว้เอง** ("ฟอร์มที่ส่งแล้วหายเงียบคือของปลอม → ปุ่มต้อง `disabled` จนกว่าจะได้ `{{COMPANY_EMAIL}}`") — การเปลี่ยนนี้ยังไม่มีใน CHANGELOG **ต้องได้ข้อสรุปก่อนขึ้นเซิร์ฟเวอร์: ต่อ `mailto:` จริง หรือปิดปุ่มกลับ**
- เฉพาะหน้าติดต่อ — **ตัวค้นหาตัวแทน `#dealers`** (`<select>` จังหวัด · "ใกล้ฉัน" · แผนที่ SVG + หมุด · URL query) รายละเอียดอยู่หัวข้อ "ตัวค้นหาตัวแทน" ข้างล่าง — **เป็นสถานะ UI ทั้งชุด ไม่นับเป็น motion**
- เฉพาะหน้าเกี่ยวกับเรา — **ตัวเลือกซีรีส์ `.pk-item` (v2.9)** `role=tablist` แนวตั้ง + roving tabindex + ArrowUp/Down/Home/End สลับ `hidden` ของ `.pk-panel` — **สถานะ UI ไม่นับเป็น motion** และอยู่ก่อน `if(reduce) return` โดยตั้งใจ คนที่ปิด motion ต้องสลับซีรีส์ได้
- เฉพาะหน้าผลงาน — แท็บ 5 ซีรีส์ (`role=tablist` + roving tabindex + ลูกศรซ้ายขวา) สลับ `hidden` ของ `.panel` — **เป็นสถานะ UI แบบเดียวกับ drawer ไม่นับเป็น motion** และอยู่ก่อน `if(reduce) return` โดยตั้งใจ
- เฉพาะหน้าเทคโนโลยี — `input[type=range]#vlt` เขียนค่าลง `--vlt` แล้ว CSS คำนวณ opacity ของ `.win-tint` ต่อ · เป็น `type="range"` ตัวเดียวของทั้งเว็บ (`grep -n 'type="range"' *.html`) — **อย่าหาด้วยเลขบรรทัด มันเลื่อนทุกรอบที่แก้ไฟล์**

**รายการข้างบนคือของที่งบ motion ข้อ 5 ใช้นับ** — จะบอกว่าหน้าไหนเกิน 4 ต้องนับจากที่นี่ ไม่ใช่จากความรู้สึก · `.drawer` มี `matchMedia('(min-width:1081px)')` คอยรีเซ็ตสถานะเมนูเวลาขยายจอ (901 → 1081 ใน v2.2) ตัวนี้ไม่นับเป็น motion · **ระวัง: `technology.html` มี `matchMedia(901)` อีกตัวเป็น gate ของไดอะแกรม sig — ตัวนั้นผูกกับ layout `.sig` ที่พับที่ 900 ห้ามเปลี่ยนตาม**

**โลโก้มีสองสถานะจากไฟล์เดียว** — `filter:brightness(0) invert(1)` บนพื้นมืด และ `none` เมื่อ nav เป็น `.solid` · **ห้ามใส่ transition บน filter** เบราว์เซอร์ interpolate filter list ข้ามสถานะไม่ได้ ทำให้โลโก้ขาวค้างบนพื้นขาวจนหาย (บั๊กนี้เจอและแก้แล้วใน v1.3) · ไฟล์ที่ใช้จริงคือ `assets/logo-finnix-web.png` เท่านั้น

**เมนูมือถือทำแล้วใน v1.5** — `.drawer` เต็มจอที่ **≤1080px ตั้งแต่ v2.2** (เดิม 900 — ย้ายเพราะ nav 7 ลิงก์ล้นช่วง 901–1080) มี JS ครบทุกไฟล์ (เปิด/ปิด · Esc · คลิกลิงก์แล้วปิด · ล็อก overflow · `aria-expanded`) นับเป็นสถานะ UI ไม่ใช่ motion จึงไม่กินงบข้อ 5

**หัวเรื่องภาษาไทยต้องดูที่ 375px เสมอ** — เบราว์เซอร์ตัดคำไทยด้วยพจนานุกรมที่ไม่มีคำทับศัพท์ เจอมาแล้วว่า "ทุกซีรีส์" กลายเป็น "ทุกซี / รีส์" ครอบด้วย `.nb{white-space:nowrap}` เมื่อเจอ **ครอบเพิ่มอีก 6 จุดใน v1.9** — ตอนนี้ใช้ครบทั้ง 6 หน้า `about` `contact` `faq` `gallery` `index` `technology` (`products` / `warranty` ถูกลบไปใน v2.6)

**ตรวจงานด้วยเบราว์เซอร์อัตโนมัติในเครื่องนี้ไม่น่าเชื่อถือ — `requestAnimationFrame` ไม่เดินเลย**
วัดแล้วพบว่า rAF loop เปล่าๆ ที่ไม่เกี่ยวกับ CSS ของหน้า วนไม่ครบ 1 วินาทีใน 45 วินาที = **เฟรมเรตแทบเป็นศูนย์ในแท็บที่ถูกสั่งงานอัตโนมัติ**
ผลที่ตามมาคือ **ตัวนับ `.cnt` ค้างกลางทาง** (เห็น `0.3 / 0.6` แทนที่จะจบที่ `3 / 7`) และ **สั่ง screenshot ผ่าน CDP ค้างครบ 30 วินาที** — ทั้งสองอย่างเป็นอาการของสาเหตุเดียวกัน **ไม่ใช่บั๊กของโค้ดตัวนับ**
การฉีด `backdrop-filter:none` ช่วยให้แคปหน้าจอผ่านได้บ่อยขึ้น จึงควรทำก่อนตรวจเสมอ **แต่ไม่ได้แก้เรื่อง rAF** — v1.8 เคยสรุปว่า `.filtered` ใน hero เป็นตัวการ **ซึ่งพิสูจน์แล้วว่าไม่ใช่**
**ตัวนับกับ motion ที่ผูกกับ scroll ต้องเปิดดูด้วยตาในเบราว์เซอร์จริงเท่านั้น อย่าตัดสินจากผลอัตโนมัติ**

**หน้าต่างเบราว์เซอร์ในเครื่องนี้ย่อต่ำกว่า 1920px ไม่ได้ — แต่ `<iframe>` วัดแทนได้ (v3.1)**
สร้าง iframe กว้างตามต้องการชี้ไปหน้าเดียวกัน iframe มี viewport ของตัวเอง media query ข้างในจึงคำนวณตามความกว้างนั้นจริง และเป็น same-origin จึงอ่าน `getComputedStyle` / `getBoundingClientRect` ข้ามเข้าไปได้ · **ใช้ยืนยันจำนวนคอลัมน์ · ความกว้างช่อง · การล้นแนวนอน ที่ 375px ได้จริง ไม่ใช่การจำลอง**
**อีกสองอย่างที่ยังพังในแท็บอัตโนมัติ นอกจาก rAF** — **transition/animation ค้างที่ค่าเริ่มต้น** ต้องฉีด `transition:none!important` ก่อนอ่าน computed style ถึงได้ค่าปลายทางจริง · **`MediaQueryList` `change` ไม่ยิงเลย แม้แต่ listener ที่วางเองสดๆ** (แปลว่า `change` listener ของ drawer ตั้งแต่ v1.5 ก็ไม่เคยถูกตรวจจริง) — ทดสอบสาขาที่ผูกกับเบรกพอยต์ด้วยการ**โหลด iframe ใหม่ที่ความกว้างเป้าหมาย**แทน
**ที่ยังต้องเปิดดูด้วยตาคนเท่านั้น** — ตัวนับ `.cnt` · ความสวยงามจริงที่ 375px · การกดด้วยนิ้ว (ค้างข้อ 13) · เวลารายงานเกณฑ์ข้อ 7 ให้เขียนว่าวัดอะไรไปแล้วบ้าง อย่าเหมารวมว่าผ่าน

**`overflow-x:auto` อย่างเดียวไม่พอในกล่องตาราง** — สเปก CSS บังคับให้ `overflow-y:visible` กลายเป็น `auto` เมื่ออีกแกนไม่ใช่ `visible` ทำให้ scrollbar แนวตั้งโผล่ทั้งที่ความสูงไม่ล้น ต้องเขียน `overflow-y:hidden` ตรงๆ (v1.6)

## ชั้นข้อมูลสินค้า

รูปโบรชัวร์ 17 หน้าใน `scans/` **ถูกถอดเป็นข้อความครบแล้ว** ไม่ต้องเปิดรูปอ่านซ้ำ — ใช้ `docs/film-data.md` (คำอธิบาย + ข้อค้นพบ) คู่กับ `data/films.json` (ค่าดิบ) แทน
5 ซีรีส์ · 3 รหัสต่อซีรีส์ · **ชื่อโฟลเดอร์ไม่ตรงกับรหัสฟิล์ม 3 ซีรีส์** — `TIT`→`SY` · `EX`→`XT` · `UV_g`→`SM` (ส่วน `CM`→`CM` · `CT`→`CT` ตรงกัน)

โครง `films.json` — `_readme` · `meta` · `series[]` (5) · `openQuestions[]` (5 ข้อ)
`meta` เก็บของที่ใช้ร่วมทั้งไลน์ (`testStandard` ยังเป็น `{{TEST_STANDARD}}` · `warrantyYears` · `priceDisplayOnSite:false` · `fields` = คำแปลไทยของทั้ง 6 ค่า) · แต่ละ `series` มี `codePrefix` `name{th,en}` `tech` `metalFree` `price` `verify` และ `variants[]` ที่มี 6 ค่าต่อรหัส — `darkness · heatReject · vlt · reflect · uv · ir`
`heatReject` คือค่าที่ตรงนิยาม TSER แต่โบรชัวร์ไม่ได้ใช้คำนั้น · `CRYSTALIZE` เป็นซีรีส์เดียวที่ `metalFree:false` · `TITANIUM` มี `tech:"{{TECH_TITANIUM}}"` และ `metalFree:null` เพราะเอกสารไม่ระบุ · มี `price` แค่ 3 ซีรีส์ (SY · CM · SM)

**`films.json` เป็นแหล่งข้อมูลตั้งต้น ไม่ใช่ไฟล์ที่หน้าเว็บ `fetch` ตอนรัน** — ข้อ 9 บังคับว่าหนึ่งหน้าจบในไฟล์เดียว ให้ฝังค่าลง HTML ตรงๆ แล้วใช้ JSON เป็นต้นทางที่ตรวจย้อนได้
ค่าที่ยังไม่มีในเอกสารเขียนเป็น `null` (JSON) หรือ `—` (md) **ห้ามเดาแทน** · ค่าที่ต้องถามลูกค้าเขียนเป็น `{{VAR}}` แม้แต่ในไฟล์ข้อมูล

`assets/finnix-*.png` คือโลโก้ชื่อซีรีส์ครบทั้ง 5 (เป็น typography ไม่ใช่ภาพถ่าย **จึงไม่ขัด MODE B**) **แต่ยังใช้ไม่ได้เพราะทุกไฟล์มีสีแดงในตัว** วางหลายอันในหน้าจอเดียวจะเกินโควตาแดงข้อ 4 หลายเท่า — ถ้าจะใช้ต้องแก้ที่โควตาแดงก่อน (v1.6) · `assets/reference-banner-lineup.jpg` เป็นภาพอ้างอิงภายใน ไม่ใช่ของขึ้นเว็บ

## ปมชื่อสินค้า — ปิดแล้วใน v1.6

**ผู้สั่งงานยืนยันให้ใช้ 5 ซีรีส์จริง ไม่ใช่ XT / CM / CL ตามบรีฟข้อ 1** — เป็นการต่างจากบรีฟโดยตั้งใจ `CL` ไม่มีอยู่จริงจึงหายไปจากเว็บทั้งหมด
~~`products.html` แสดงค่าจริงครบ 15 รหัส~~ **หน้านั้นถูกลบใน v2.6** · กติกาเดิมยังใช้กับตัวเลขที่เหลือ: **ถ้าแก้ค่าใดต้องแก้ที่ `films.json` ก่อนแล้วค่อยลงหน้าเว็บ ไม่ใช่แก้ที่ HTML ตรงๆ**

`#series` ในหน้าแรกที่เคยเขียน XT เป็น "Flagship" (ทั้งที่ EXTRA CLEAR ลดความร้อนต่ำที่สุดในไลน์) **ถูกลบทั้งบล็อกแล้ว** พร้อม CSS ที่ไม่มีใครใช้ — ค่าสมมติ 12 จุดที่เคยค้างตั้งแต่ v1.0 หายไปพร้อมบล็อกนี้

**ช่องภาพบนการ์ด `/gallery` เป็น "กระจกจำลอง" ตั้งแต่ v3.2** — เดิมเป็นกล่องเทาเปล่าสูง 160px เหมือนกันหมด 20 ใบ · ตอนนี้ความเข้มมาจาก `--vlt` ของ**รหัสฟิล์มบานข้าง**ของการ์ดใบนั้น ผ่านสูตรเดียวกับ `.win-tint` ที่ `/technology` (`opacity:calc(1 - var(--vlt)/100)`) · gradient ล้วน **ไม่ขัด MODE B** และไม่มี transition จึงไม่กินงบ motion
**ป้ายชื่อซีรีส์เป็นชิปพื้น `--ink` ไม่ใช่ตัวหนังสือลอย** เพราะพื้นหลังแต่ละใบเข้มไม่เท่ากัน (0.65–0.95) ตัวหนังสือลอยจะตก contrast บนใบที่ฟิล์มอ่อน — บทเรียนเดียวกับ `.win-tag`
**`.cat-item` ในหน้าแรกมีแถบเทียบตั้งแต่ v3.2** สเกล 0–100% เท่ากันทุกใบ แพทเทิร์นเดียวกับ `.pk-bar` ที่ `/about` · **ทั้งสองอย่าง generate จาก `films.json` ไม่ได้พิมพ์มือ และมีสคริปต์เทียบให้แล้ว**

**accordion ที่ `/faq` กับ FAQ ย่อในหน้าแรกไล่ความสูงตอนเปิด-ปิดตั้งแต่ v3.2** — `::details-content` + `interpolate-size:allow-keywords` ที่ `:root` (ใส่ครบ 6 ไฟล์เพราะ `:root` ต้องแฮชตรงกัน) · ห่อด้วย `@supports` เบราว์เซอร์ที่ไม่รองรับจะเปิดทันทีแบบเดิม **วัดแล้วว่าสถานะปลายทางเท่ากับตอนไม่มีกฎนี้เลย เนื้อหาจึงไม่มีทางถูกซ่อน**
**นับเป็นสถานะ UI ไม่ใช่ motion ข้อ 5** กติกาเดียวกับ drawer ที่มี transition .4s และไม่เคยถูกนับ

**เนื้อหาที่ไม่มีที่มาเหลือสองที่** (เงื่อนไขรับประกันหายไปพร้อมหน้าใน v2.6) — **รายชื่อตัวแทน 9 รายใน `contact.html` (ค้างข้อ 18 — `placeholder:true` ทุกราย · เบอร์ `081000000x` เป็นเบอร์ปลอมที่ยังกดโทรได้)** · **รายการตัวอย่าง 20 คันใน `gallery.html` (v2.2 ติดป้าย "ตัวอย่างการแสดงผล" ครบตามกติกา MODE B — ห้ามขึ้นเซิร์ฟเวอร์จริงทั้งที่ป้ายยังอยู่ ค้างข้อ 17)** (หน้าแรกกับ `#work` ปิดไปใน v1.8) · รายละเอียดทุกข้ออยู่ในหัวข้อ "ค้างอยู่" ของ `docs/CHANGELOG.md` ซึ่งบังคับให้อ่านก่อนเริ่มงานอยู่แล้ว **อย่าเชื่อตัวเลขบนหน้าเว็บก่อนเทียบกับที่นั่น**

## หน้าที่ทำตามแบบของคู่แข่ง — `about` (v1.8 · **จัดดีไซน์ใหม่ตาม `spms-est.com/about-us/` ใน v2.9**) · `contact` (v2.0/v2.2 · ลดของลง v2.3 · **รื้อตามหน้าต้นแบบทั้งหน้า v2.4**) · `gallery` (v2.1/v2.2)

**v2.9 `/about` ใช้หน้าต้นแบบตัวที่สอง — `spms-est.com/about-us/` ซึ่งเป็นเว็บผู้จัดจำหน่าย 3M** จึงชนข้อ 2.9 ตรงตัวยิ่งกว่า solargard · ผู้สั่งงานสั่งเอง เส้นแบ่งเดิมยังใช้: เอาลำดับและวิธีจัดแถว-คอลัมน์มา **ห้ามลอกกริดหรือ component**
สิ่งที่หน้านั้นมีแต่เราทำไม่ได้และ**ตัดสินไปแล้ว อย่ารื้อซ้ำ** — บล็อก CEO พื้นแดงเต็มความกว้าง + ภาพผู้บริหาร (MODE B + โควตาแดง) · ภาพอาคารและภาพ CTA (MODE B) · VISION/MISSION กับไทม์ไลน์ปีและตัวเลขจำนวนร้าน (**ไม่มีข้อมูลเลย ใส่ไปคือแต่งขึ้น**) · ถ้าได้ข้อมูลมาเติมได้โดยไม่ต้องรื้อ

**ผู้สั่งงานสั่งให้ใช้ `solargard.co.th/about` เป็นแบบ ซึ่งชนกับข้อ 2.9 ตรงตัว** — เส้นแบ่งที่ตัดสินไว้ใน v1.8 คือ เอา**ลำดับหัวข้อและสิ่งที่หน้านั้นพูดถึง**มาใช้ได้ (ข้อ 1 อนุญาตให้ใช้เป็นโครงสร้าง IA) แต่ layout ทุกชิ้นสร้างจาก token ของเราเอง **ห้ามลอกกริด สัดส่วน หรือ component ของเขา**
โครง 5 ส่วน — จุดขายแบรนด์ · แหล่งผลิต · จุดเด่นเชิงเทคนิค · การรับรอง · CTA
**ส่วนที่ 2 กับ 4 เป็นบล็อกแจ้งล้วน ยังไม่ใช่เนื้อหา** เพราะรอ `{{FILM_SOURCE}}` กับ `{{CERTIFICATIONS}}` — โครงวางไว้ให้เติมได้โดยไม่ต้องรื้อ
ส่วนที่ 3 เป็น**ช่วงค่าระดับซีรีส์** (อ่อนสุด–เข้มสุด) จาก `data/films.json` — **แก้ค่าต้องแก้ที่ `films.json` ก่อนเสมอ** และมีสคริปต์ตรวจให้แล้วข้างบน
**v2.9 เปลี่ยนจาก `<table class="spec">` 5 คอลัมน์เป็น `.pick` — ซ้ายเลือกซีรีส์ ขวาอ่านค่า** (`role=tablist` แนวตั้ง + roving tabindex + ArrowUp/Down/Home/End) · **markup ทั้งบล็อก generate จาก `films.json` ไม่ได้พิมพ์มือ** ถ้าจะแก้ ให้ generate ใหม่อย่าแก้ทีละตัว
ใต้ตัวเลขที่เป็นช่วงมี `.pk-bar` แสดงช่วงบน**สเกล 0–100% เท่ากันทุกซีรีส์** เพื่อให้ยังเทียบข้ามซีรีส์ได้หลังเปลี่ยนเป็นแท็บ · **ค่ายูวีไม่มีแถบโดยตั้งใจ** เพราะเป็นค่าเดียวทุกรหัส และแถบจะสื่อว่า "มาก=ดี" ซึ่งไม่จริงกับแสงส่องผ่าน
**`.sec-head.bar` กับ `.pick` เป็นของเฉพาะหน้านี้** — `.sec-head` เปล่าๆ ยังเป็นของร่วมกับ `index`/`technology` **อย่าแก้ตัวนั้น** · แถบนำหัวข้อเป็น `--ink` ไม่ใช่แดงแบบหน้าต้นแบบ เพราะหนึ่งหน้าจอเห็นแถบได้พร้อมกันสองอันและข้อ 4 ไม่อนุญาตแดงชนิดนี้
**ไอคอนใน `.why-cell` เป็น `--ink` ตั้งแต่ v2.9** — เดิมเป็น `var(--red)` ทั้ง 4 ใบซึ่งเกินโควตาข้อ 4 มาตั้งแต่ v1.8 **ห้ามเปลี่ยนกลับจนกว่าจะแก้ที่โควตา**
**`.why` อยู่ใน `.wrap` แล้ว** — เดิม full-bleed เต็มจอทำให้ขอบซ้ายไม่ตรงกับหัวข้อ **อย่าย้ายออกอีก**

**ราคาไม่ขึ้นเว็บโดยตั้งใจ** มีข้อมูลอยู่ใน `films.json` แค่ 3 ซีรีส์ และข้อ 1 ระบุว่าเว็บนี้ไม่ใช่ร้านค้า

## หน้าติดต่อ — รื้อตามหน้าต้นแบบทั้งหน้าใน v2.4

**`contact.html` เป็นหน้าที่ถูกสั่งให้ทำตาม `solargard.co.th/contact` แบบตรงตัว** (คำสั่ง "เอาตามนั้นเลยๆ แบบแปะๆ") — โครงหน้าทุกส่วนมาจากหน้าต้นแบบ **ก่อนจัดวางอะไรใหม่ในหน้านี้ ให้เปิดหน้าต้นแบบเทียบก่อน** ไม่ใช่ตัดสินจากรสนิยม
ลำดับ 4 ส่วน — **hero สองคอลัมน์ (ข้อมูลติดต่อซ้าย · ฟอร์มขวา) → "ค้นหาตัวแทน | OR | สำนักงานใหญ่" → แผนที่ซ้าย + รายการขวา → ปิดท้าย**

**สามอย่างที่หน้าต้นแบบมีแต่เราทำตามไม่ได้ — ตัดสินแล้วใน v2.4 อย่ารื้อซ้ำ**
| ของเขา | ของเรา | เหตุผล |
|---|---|---|
| Google Maps ฝังในหน้า | **SVG ประเทศไทยวาดเองในไฟล์** | ข้อ 9 อนุญาต request ภายนอกแค่ Google Fonts |
| ภาพแผนที่โลกเป็นพื้นหลัง hero | gradient | MODE B ห้ามภาพถ่าย |
| รายชื่อตัวแทนจริง ~100 ร้าน | ข้อมูลสมมติ 9 ราย + ป้ายกำกับ | เป็นเครือข่ายของบริษัทอื่น เอามาแสดงคือแอบอ้าง |

**แผนที่เป็น Google Maps จริงตั้งแต่ v2.5** — `<iframe id="gmap">` ชี้ endpoint embed ที่ไม่ต้องใช้ API key · **นี่คือ request ภายนอกตัวที่สองของทั้งเว็บ และเป็นข้อยกเว้นข้อ 9 ที่ผู้สั่งงานอนุมัติเอง** (v2.4 เคยวาดแผนที่เป็น SVG เองเพื่อเลี่ยงข้อนี้ — ถูกแทนแล้ว โค้ด `xy()` กับ `drawPins()` ถูกลบ)
`setMap(q, zoom, cap, outQ)` เป็นทางเดียวที่เปลี่ยนแผนที่ · ระดับซูมที่ใช้: **ทั้งประเทศ 5 · จังหวัด 10 · รอบตำแหน่งผู้ใช้ 11 · ร้านเดียว 16** · ทุกครั้งต้องอัปเดตคำบรรยายใต้แผนที่ (`#map-cap`) และลิงก์ "เปิดใน Google Maps" (`#map-out`) ไปพร้อมกัน ไม่งั้นคำบรรยายจะโกหกว่ากำลังดูอะไรอยู่
**หมุดบนแผนที่เป็นของ Google ไม่ใช่ของเรา** — สีแดงของหมุดคุมไม่ได้และไม่นับในโควตาข้อ 4 (เป็นเนื้อหาบุคคลที่สาม)
`iframe{height:670px}` ให้กล่องซ้ายขวาสูงเท่ากัน · `loading="lazy"` **ห้ามถอด** ไม่งั้นแผนที่จะถ่วงตอนเปิดหน้า
**ปุ่ม "ดูบนแผนที่" อยู่ในแถวปุ่มของการ์ดทุกใบ** — ไม่ทำทั้งการ์ดเป็น `role=button` เพราะการ์ดมีลิงก์อยู่ข้างในแล้ว (interactive ซ้อน interactive ใช้กับคีย์บอร์ดไม่ได้) · คลิกทั้งการ์ดยังใช้ได้สำหรับเมาส์ ผ่าน handler ที่ข้ามเมื่อคลิกโดน `<a>`

**ข้อมูลอยู่สองที่และต้องตรงกันเสมอ** — `data/dealers.json` เป็นต้นทาง หน้าเว็บอ่านจาก `<script id="dealers-data" type="application/json">` ที่ฝังไว้ก่อน `<script>` หลัก **ไม่ `fetch` ตอนรัน (ข้อ 9)** · มีสคริปต์เทียบให้แล้วข้างบน
**v2.4 เคยทำบล็อกนี้หลุดหายตอนรื้อ markup แล้วทั้งหน้าตาย** — กู้คืนได้ในคำสั่งเดียวเพราะมี JSON เป็นต้นทาง **นี่คือเหตุผลที่ต้องแยกไฟล์ข้อมูลไว้ อย่าแก้ที่ HTML ตรงๆ**
โครงต่อหนึ่งราย — `id · name · companyName · tier · services[] · address · province · region · phone · lineId · lat · lng · hours · featured · placeholder`
`tier` **3 ค่าตั้งแต่ v2.6** (`center` `automotive` `sub`) มีแบดจ์ของตัวเองบนการ์ด — **แยกด้วยข้อความ ไม่ใช่สีอย่างเดียว และไม่ใช้แดงเลยเพราะโควตาข้อ 4** · `services` **2 ค่า** (`automotive` `ppf`)
**`building` ถูกตัดออกทั้งระบบใน v2.6** (แท็บใน `/gallery` · `SVC_TH`/`TIER_TH` · ข้อมูลใน `dealers.json` · CSS `.badge.building`) — ตัวแทนที่ทำฟิล์มอาคารอย่างเดียวถูกถอดออกจากรายชื่อ เหลือ 9 ราย · **JS กรอง service/tier ที่ไม่มีคำแปลออกอยู่แล้ว ถ้าเพิ่มค่าใหม่ใน JSON ต้องเพิ่มคำแปลด้วย ไม่งั้นจะหายเงียบ**

ส่วนประกอบที่เหลือ — ทั้งชุดเป็น**สถานะ UI ไม่ใช่ motion** (contact นับได้ 2 อย่าง = text reveal + `.rv`)
- **`<select>` จังหวัด** สร้าง option จาก `dealers.json` ตอนเปิดหน้า — **มีเฉพาะจังหวัดที่มีตัวแทนจริง** ไม่ใช่ 77 จังหวัด (แบบเดียวกับหน้าต้นแบบ) · v2.3 เคยเป็น combobox ARIA เขียนเอง **ถูกลบทิ้งใน v2.4 อย่าเขียนกลับ** — `<select>` ของระบบดีกว่าบนมือถือและไม่ต้องดูแล ARIA เอง
- **ปุ่ม "ค้นหา"** ซ้ำซ้อนกับ `<select>` ที่กรองทันทีอยู่แล้ว — **มีเพราะหน้าต้นแบบมี** ไม่ใช่เพราะจำเป็น
- **"ใกล้ฉัน"** `navigator.geolocation` → เรียงตามระยะทาง haversine · **ถูกปฏิเสธต้องไม่พังหน้า** — ขึ้น `.toast` แล้วใช้ dropdown ต่อได้ · หน้าต้นแบบไม่มีปุ่มนี้ เราเก็บไว้เอง
- **URL เก็บสถานะแทน storage** — `history.replaceState` เขียน `?province=...#dealers` **เฉพาะตอนเลือกจังหวัดจริง** · ถ้าไม่มีตัวกรอง **ห้ามแตะ hash** (บั๊ก v2.3 ที่ลิงก์ `#inquiry` ถูกดึงไป `#dealers`)
- **ฟอร์ม 3 ช่อง (ชื่อ · อีเมล · ข้อความ)** ตามหน้าต้นแบบ — **v2.4 ตัดช่องเบอร์โทรที่เคยบังคับกับ `thPhoneOk()` ออก** แปลว่าคนที่ไม่มีอีเมลติดต่อกลับไม่ได้ · จะเอากลับ เพิ่มช่องเดียวและคืนฟังก์ชันได้ทันที
- **ปุ่มส่งเปิดอยู่และเป็น "โหมดสาธิต"** (`setTimeout` 600ms → `.sent`) ไม่มีปลายทางรับจริง · **ขัดกับข้อกำหนดเดิมที่ v2.2 ตั้งไว้เอง ยังไม่มีข้อสรุป** — ต้องต่อ `mailto:` จริงหรือปิดปุ่มกลับก่อนขึ้นเซิร์ฟเวอร์
- **`.toast` ดูเหมือน CSS ตายแต่ไม่ตาย** — JS สร้างด้วย `className` **ห้ามลบ**

**การ์ดพื้นสว่างในโซนพื้นมืดต้องระวัง selector ของ hero** — `.page-head p` ทำให้ข้อความในการ์ดฟอร์มกลายเป็นสีขาวบนพื้นขาวจนหายไปทั้งบรรทัด (เจอและแก้ใน v2.4 ด้วย `.cform .form-note` ที่ specificity สูงกว่า) · **เพิ่ม `<p>` ในการ์ดเมื่อไหร่ ให้เช็คสีที่ได้จริงทุกครั้ง**

**สำนักงานใหญ่อยู่ที่เดียว** — คอลัมน์ขวาของแถบ "ค้นหาตัวแทน | OR | สำนักงานใหญ่" (ที่อยู่ · โทรศัพท์ · อีเมล · เวลาทำการ · เลขทะเบียน) **ก่อนเพิ่มข้อมูลบริษัทที่ไหนในหน้านี้ ให้เช็คที่นี่ก่อน**
**เบอร์โทรจริงเป็นค่าตายตัว 2 จุดในหน้านี้** (`tel:0635978497` — กล่องช่องทางใน hero กับคอลัมน์สำนักงานใหญ่) เป็นจุดเดียวของทั้งเว็บที่มีเบอร์จริงแทน `{{VAR}}`

## หนี้ที่ต้องแก้พร้อมกันทุกไฟล์

~~**ลำดับหัวข้อข้ามระดับ**~~ **ปิดแล้วใน v3.0** — footer เป็น `h3` ทุกไฟล์ · `contact` แก้ `h1`→`h3` เป็น `h2` · `gallery` เพิ่ม `h2` ซ่อนสายตาใน panel · ไล่ทั้ง 6 ไฟล์แล้วไม่มีการข้ามระดับเหลือ (วัดซ้ำ 26 ส.ค. 2569 ด้วย `grep -o "<h[1-6]" *.html`)
**หนี้ข้ามไฟล์ที่ยังเหลือคือระยะที่หารด้วย 8 ไม่ลงตัว** — v3.0 ไล่ทั้ง 6 ไฟล์พบ **14 ค่า** (`10 · 12 · 13 · 14 · 20 · 22 · 28 · 30 · 44 · 52 · 60 · 76 · 116 · 132`) มากกว่า 4 จุดที่เคยบันทึกไว้สามเท่า · **ยังไม่ตัดสินว่าจะแก้ให้ลงตัวหรือรับเป็นข้อยกเว้นแล้วบันทึก** (ค้างข้อ 8) — ถ้าจะแก้ ต้องแก้ครบ 6 ไฟล์รอบเดียว
รายการค้างที่เหลือทั้งหมดอยู่ท้าย `docs/CHANGELOG.md` หัวข้อ "ค้างอยู่ — ต้องทำต่อ" (**21 ข้อ**) — ที่นั่นคือรายการจริง ไฟล์นี้ไม่เก็บซ้ำ · **ข้อ 1 กับ 3 เลื่อนระดับเป็นบล็อกการขึ้นเซิร์ฟเวอร์แล้วใน v2.3** เพราะบล็อกแจ้งบนหน้าถูกลบทั้งที่เนื้อหายังไม่ยืนยัน

## วิธีทำงานที่คาดหวัง

ตาม `docs/master-prompt.md` ข้อ 12 — **ทวนโจทย์ให้ยืนยันก่อนเขียนโค้ดเสมอ ยกเว้นสั่งว่า "ทำเลย"** จากนั้นระบุโหมด A/B → อธิบายการตัดสินใจหลัก 3 ข้อพร้อมเหตุผล (ไม่ใช่รายการฟีเจอร์) → ส่งไฟล์ → แนบตัวแปรที่ต้องขอ → บอกตรงๆ ว่าจุดไหนยังอ่อน
ตอบเป็นภาษาไทย เนื้อหาบนเว็บเป็นภาษาไทยทั้งหมด (โครงสร้างต้องรองรับ EN ในเฟสถัดไป แต่ยังไม่ได้วาง)
ก่อนส่งงานต้องตรวจเกณฑ์ 9 ข้อ (ข้อ 11) ทีละข้อแล้วรายงานผล ไม่ใช่สรุปรวม — แล้วบันทึกลง `docs/CHANGELOG.md` พร้อมอัปเดตหัวข้อ "ค้างอยู่" และ "ตัวแปรที่ต้องขอ"
