# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ภาพรวม

เว็บไซต์ Brand & Corporate ของ บริษัท ฟินนิกซ์ ฟิล์ม จำกัด (ฟิล์มกรองแสงรถยนต์) — static site ล้วน
**ไม่มี build system · ไม่มี package manager · ไม่มี test · ไม่มี git** หนึ่งหน้า = หนึ่งไฟล์ HTML ที่มี CSS และ JS อยู่ในไฟล์เดียวกัน
ทำแล้ว 7 หน้าจาก 10 หน้าตามข้อ 8 ของบรีฟ — `index.html` (`/`) · `technology.html` · `products.html` · `about.html` · `warranty.html` · `faq.html` · `contact.html`
ยังขาด `/products/[series]` `/gallery` `/news` — เหตุผลอยู่ใน CHANGELOG v1.5 และ v1.6 (หน้ารายซีรีส์อาจไม่จำเป็นแล้วหลัง v1.6 · **ยังรอการตัดสินใจ**)

## อ่านก่อนเริ่มงานทุกครั้ง

| ไฟล์ | บทบาท |
|---|---|
| `docs/master-prompt.md` | บรีฟหลัก v1.0 — **เป็นข้อผูกมัด** งานทุกชิ้นถูกตัดสินด้วยข้อที่มีหมายเลข (ข้อ 1–12) อ้างอิงเป็น "ข้อ N" เสมอ · ข้อ 2 คือ anti-brief ถ้าละเมิดคือรื้อทำใหม่ |
| `docs/CHANGELOG.md` | บันทึกการตัดสินใจ **ปัจจุบันถึง v1.6 และตรงกับไฟล์จริง** · เพิ่มหัวข้อใหม่ไว้**บนสุด** · ท้ายไฟล์มี "ค้างอยู่ 11 ข้อ" กับ "ตัวแปรที่ต้องขอ" ที่ต้องอัปเดตทุกครั้ง — **อ่านสองหัวข้อนี้ก่อนเริ่มงานเสมอ อย่าลอกรายการค้างมาไว้ที่นี่** |
| `docs/film-data.md` | **ค่าสเปกจริงทั้ง 15 รหัสฟิล์ม** ถอดจากโบรชัวร์ใน `Data/` · อธิบายโครงข้อมูล ข้อค้นพบที่ใช้เป็นเนื้อหาได้ และ 9 ข้อที่ต้องยืนยันกับลูกค้า — อ่านก่อนแตะตัวเลขใดๆ บนหน้าเว็บ |
| `Data/films.json` | ข้อมูลชุดเดียวกันในรูปแบบที่เครื่องอ่านได้ — **เป็น source of truth ของตัวเลขทุกตัวบนเว็บ** |
| `review.html` | หน้าตรวจงานภายใน `noindex,nofollow` ไม่มีหน้าไหนลิงก์ถึง — **ห้ามขึ้นเซิร์ฟเวอร์จริง** · **ค้างที่ v1.2 ตามหลัง 4 เวอร์ชัน** ยังไม่รู้จักหน้าใหม่ 5 หน้า · รายการตัวแปรยังเป็นชุด XT/CM/CL ที่เลิกใช้แล้ว · ลิงก์ `assets/logo-finnix.png` เสียเพราะไฟล์ถูกลบ |

ดัชนีข้อของบรีฟ (ไว้เปิดตรงจุด ไม่ต้องอ่านทั้งไฟล์) — 1 โจทย์ธุรกิจ · 2 anti-brief · 3 จุดยืนแบรนด์+น้ำเสียง · 4 design tokens · 5 งบ motion · 6 signature · 7 MODE A/B · 8 โครงสร้างเว็บ+IA · 9 ข้อกำหนดเทคนิค · 10 กติกาข้อมูลที่ยังไม่มี · 11 เกณฑ์ตรวจรับ 9 ข้อ · 12 รูปแบบการตอบ

## คำสั่ง

ไม่มี build / lint / test — ตรวจงานด้วยตาและด้วยเกณฑ์รับงาน 9 ข้อ (ข้อ 11) เท่านั้น

```powershell
python -m http.server 8000     # เสิร์ฟจาก root แล้วเปิด http://localhost:8000/
```

เปิดด้วย `file://` ก็ได้ แต่ควรใช้ local server เวลาทดสอบ motion ที่ผูกกับ scroll
ต้องตรวจสองความกว้างเสมอ — **1440px และ 375px** (จุดตัด 1080 / 900 / 620)
เป้า Lighthouse ≥ 95 ทั้งสี่หมวดตามข้อ 9 — **ยังไม่เคยวัดจริง**

**สคริปต์ตรวจเทียบ `products.html` กับ `films.json` ไม่ได้ถูกเก็บไว้** — v1.6 เขียนขึ้นใช้ครั้งเดียวแล้วทิ้ง ถ้าแก้ตัวเลขต้องเขียนใหม่ทุกครั้ง (โฟลเดอร์นี้ไม่มีไฟล์สคริปต์ใดเลยนอกจาก `films.json`)

## กฎที่ละเมิดแล้วงานไม่ผ่าน

- **ไฟล์เดียวจบต่อหนึ่งหน้า** — request ภายนอกมีได้แค่ Google Fonts เท่านั้น ห้ามดึงไลบรารี (เหตุผลที่ signature เป็น SVG ไม่ใช่ WebGL อยู่ใน CHANGELOG v1.1)
- **ฟอนต์ Anuphan (หัวเรื่อง) + IBM Plex Sans Thai (เนื้อหา) เท่านั้น** ฟอนต์ที่ไม่มีชุดภาษาไทยห้ามใช้เด็ดขาด
- **Motion ไม่เกิน 4 อย่างต่อหน้า** และต้องเคารพ `prefers-reduced-motion` (JS ทุกไฟล์แตกสาขาด้วยตัวแปร `reduce`) — **`index.html` มี 5 อย่างคือเกินอยู่** (text reveal · scroll reveal `.rv` · ตัวเลื่อนเทียบใน hero · ตัวนับ `.cnt` · ไดอะแกรมชั้นฟิล์มผูก scroll) · หน้าอื่นมี 2 อย่าง เหลืองบอีก 2
- **สีแดงไม่เกิน 2 จุดต่อหนึ่งหน้าจอ** ใช้ได้เฉพาะปุ่มหลัก · จุด eyebrow · ชั้นเซรามิกในไดอะแกรม · focus/error
- **ทุกระยะหารด้วย 8 ลงตัว** ข้อยกเว้นที่ยอมรับแล้วมีบันทึกไว้ใน CHANGELOG v1.1 (เช่น `padding:132px` ที่บรีฟระบุมาเองแต่หารไม่ลงตัว)
- **ห้ามแต่งตัวเลขสเปก** ค่าที่ยังไม่มีต้องเขียนเป็น `{{VAR}}` และต้องไปเพิ่มในรายการตัวแปรทั้งใน CHANGELOG และ `review.html`
- **มีค่าจริงแล้วก็ยังขึ้นไม่ได้ จนกว่าจะได้ `{{TEST_STANDARD}}`** ข้อ 3 บังคับว่าทุกตัวเลขต้องบอกได้ว่าวัดด้วยมาตรฐานใด โบรชัวร์เขียนแค่ "ตามมาตรฐานสากลของโลก" — **นี่คือตัวปลดล็อกตัวเดียวที่กั้นตัวเลขทั้งชุดอยู่** ตอนนี้ `products.html` มีบล็อกแจ้งกำกับไว้บนหน้าแทน
- **ห้ามใช้ localStorage / sessionStorage**
- **Signature มีได้ชิ้นเดียวทั้งเว็บ** = ภาพตัดขวางชั้นฟิล์มในส่วนที่ 3 ของ `technology.html` · `index.html` มีซ้ำอยู่ที่ `#tech` ซึ่งต้องถอดออก
- **CTA เดียวต่อหน้า** ปลายทางเดียวของทั้งเว็บคือ `https://finnixcenterth.com` · **ปุ่มแดงมีได้ปุ่มเดียวต่อหน้าจอ และ nav CTA กินโควตานั้นไปแล้ว** 5 หน้าใหม่จึงใช้ `.btn-light` (ขาวบนพื้นมืด) ในส่วนปิดท้าย ห้ามเปลี่ยนกลับเป็นแดง — `index.html` กับ `technology.html` ยังใช้ `.btn-red` ปิดท้ายอยู่ คือส่วนที่ยังเกินโควตา
- **ห้ามคัดลอก layout ของ 3M / Solar Gard** (ข้อ 2.9) — แต่ข้อ 1 อนุญาตให้ใช้เป็น**โครงสร้าง IA** ได้ เส้นแบ่งอยู่ตรงนี้และเคยถูกถามมาแล้ว บันทึกการตัดสินอยู่ใน CHANGELOG v1.5
- ทุกหน้าทำในโหมด **MODE B** (ไม่มีงบภาพ) — ไม่มีภาพถ่ายเลย มีแต่โลโก้ ที่เหลือเป็น SVG และ typography · ห้ามใช้ภาพ stock

## โครงสร้างโค้ดที่ใช้ซ้ำทุกหน้า

ทั้ง 7 ไฟล์ใช้โครงเดียวกันและ **คัดลอกกันมาทั้งบล็อก** — nav, drawer, footer, `:root` tokens, `.rv`, `.var`, ปุ่ม `.btn`
`:root` เหมือนกันทุกบรรทัดทุกไฟล์ (ตรวจแล้ว hash ตรงกันทั้ง 7) **แก้ที่ใดต้องแก้ครบทั้ง 7 ไฟล์พร้อมกันเสมอ** ไม่งั้น nav จะขยับไม่เท่ากันเวลาสลับหน้า
ต้นทุนของการซ้ำสูงขึ้นเรื่อยๆ (จาก 2 เป็น 7 จุดต่อการแก้หนึ่งครั้ง) แต่ข้อ 9 บังคับว่าหนึ่งหน้าต้องจบในไฟล์เดียว จึงยังแยกไฟล์ร่วมไม่ได้ · nav/footer เหมือนกันทุกไฟล์ ส่วน `.drawer` ต่างกันเล็กน้อยตรงสถานะลิงก์ปัจจุบัน

ลำดับ section — nav ลอย → hero พื้น `--void` → สลับพื้น `--paper` / `--smoke` → section ปิดท้ายพื้นมืด + CTA → footer
5 หน้าใหม่ใช้ `.page-head` (hero แบบสั้น padding 200px) แทน hero เต็มจอ และ **ไม่มี signature เลย** ตามข้อ 6 ที่ห้ามสร้างของว้าวชิ้นที่สอง

IA ตาม 3 แกนของข้อ 8 — nav มี 5 ลิงก์ (เกี่ยวกับเรา · เทคโนโลยี · ผลิตภัณฑ์ · รับประกัน · ติดต่อ) + CTA แดง · FAQ อยู่ใน footer และ drawer
ลิงก์ทุกจุดชี้ไฟล์จริงหมดแล้วตั้งแต่ v1.6 · **เหลือลิงก์ตายจุดเดียว** — `index.html:421` ปุ่ม "ดูผลงานทั้งหมด" ยังเป็น `href="#"` (จะหายไปพร้อมกับ `#work` ถ้าถอดทั้ง section)

JS เป็น IIFE ตัวเดียวท้ายไฟล์ ไม่มี dependency แพตเทิร์นที่ใช้ซ้ำ
- `.loaded` บน body สั่ง text reveal ตอนเข้าหน้า (event `load` + `setTimeout` 400ms กันพลาด)
- `nav.solid` สลับเมื่อ `scrollY > 40` — ตัวนี้คือที่มาของสถานะโลโก้สองแบบ
- IntersectionObserver จับ `.rv` ทำ scroll reveal · stagger 60ms แบบ `(i % 4)`
- ไดอะแกรมชั้นฟิล์มผูกกับ scroll — คำนวณ progress จาก `getBoundingClientRect()` ของ section แล้ว `translateY` แต่ละ `.layer` พร้อมติด `.on` ให้รายการ legend (`#stack`/`#legend` ในหน้าแรก · `#sig`/`#sig-legend` ในหน้าเทคโนโลยี)
- เฉพาะหน้าแรก — ตัวนับ `.cnt[data-to]` และตัวเลื่อนเทียบก่อน-หลังใน hero ที่ใช้ pointer event เขียนค่าลง custom property `--split`
- เฉพาะหน้าเทคโนโลยี — `input[type=range]#vlt` เขียนค่าลง `--vlt`

**โลโก้มีสองสถานะจากไฟล์เดียว** — `filter:brightness(0) invert(1)` บนพื้นมืด และ `none` เมื่อ nav เป็น `.solid` · **ห้ามใส่ transition บน filter** เบราว์เซอร์ interpolate filter list ข้ามสถานะไม่ได้ ทำให้โลโก้ขาวค้างบนพื้นขาวจนหาย (บั๊กนี้เจอและแก้แล้วใน v1.3) · ไฟล์ที่ใช้จริงคือ `assets/logo-finnix-web.png` เท่านั้น

**เมนูมือถือทำแล้วใน v1.5** — `.drawer` เต็มจอที่ ≤900px มี JS ครบทุกไฟล์ (เปิด/ปิด · Esc · คลิกลิงก์แล้วปิด · ล็อก overflow · `aria-expanded`) นับเป็นสถานะ UI ไม่ใช่ motion จึงไม่กินงบข้อ 5

**หัวเรื่องภาษาไทยต้องดูที่ 375px เสมอ** — เบราว์เซอร์ตัดคำไทยด้วยพจนานุกรมที่ไม่มีคำทับศัพท์ เจอมาแล้วว่า "ทุกซีรีส์" กลายเป็น "ทุกซี / รีส์" ครอบด้วย `.nb{white-space:nowrap}` เมื่อเจอ (ใช้อยู่แล้วใน `warranty.html` และ `products.html`)

**`overflow-x:auto` อย่างเดียวไม่พอในกล่องตาราง** — สเปก CSS บังคับให้ `overflow-y:visible` กลายเป็น `auto` เมื่ออีกแกนไม่ใช่ `visible` ทำให้ scrollbar แนวตั้งโผล่ทั้งที่ความสูงไม่ล้น ต้องเขียน `overflow-y:hidden` ตรงๆ (v1.6)

## ชั้นข้อมูลสินค้า

รูปโบรชัวร์ 17 หน้าใน `Data/` **ถูกถอดเป็นข้อความครบแล้ว** ไม่ต้องเปิดรูปอ่านซ้ำ — ใช้ `docs/film-data.md` (คำอธิบาย + ข้อค้นพบ) คู่กับ `Data/films.json` (ค่าดิบ) แทน
5 ซีรีส์ · 3 รหัสต่อซีรีส์ · **ชื่อโฟลเดอร์ไม่ตรงกับรหัสฟิล์ม 3 ซีรีส์** — `TIT`→`SY` · `EX`→`XT` · `UV_g`→`SM` (ส่วน `CM`→`CM` · `CT`→`CT` ตรงกัน)

โครง `films.json` — `_readme` · `meta` · `series[]` · `openQuestions[]`
`meta` เก็บของที่ใช้ร่วมทั้งไลน์ (`testStandard` ยังเป็น `{{TEST_STANDARD}}` · `warrantyYears` · `fields` = คำแปลไทยของทั้ง 6 ค่า) · แต่ละ `series` มี `codePrefix` `tech` `metalFree` `price` `verify` และ `variants[]` ที่มี 6 ค่าต่อรหัส — `darkness · heatReject · vlt · reflect · uv · ir`
`heatReject` คือค่าที่ตรงนิยาม TSER แต่โบรชัวร์ไม่ได้ใช้คำนั้น · `CRYSTALIZE` เป็นซีรีส์เดียวที่ `metalFree:false` · `TITANIUM` มี `tech:"{{TECH_TITANIUM}}"` และ `metalFree:null` เพราะเอกสารไม่ระบุ

**`films.json` เป็นแหล่งข้อมูลตั้งต้น ไม่ใช่ไฟล์ที่หน้าเว็บ `fetch` ตอนรัน** — ข้อ 9 บังคับว่าหนึ่งหน้าจบในไฟล์เดียว ให้ฝังค่าลง HTML ตรงๆ แล้วใช้ JSON เป็นต้นทางที่ตรวจย้อนได้
ค่าที่ยังไม่มีในเอกสารเขียนเป็น `null` (JSON) หรือ `—` (md) **ห้ามเดาแทน** · ค่าที่ต้องถามลูกค้าเขียนเป็น `{{VAR}}` แม้แต่ในไฟล์ข้อมูล

`assets/finnix-*.png` คือโลโก้ชื่อซีรีส์ครบทั้ง 5 (เป็น typography ไม่ใช่ภาพถ่าย **จึงไม่ขัด MODE B**) **แต่ยังใช้ไม่ได้เพราะทุกไฟล์มีสีแดงในตัว** วางหลายอันในหน้าจอเดียวจะเกินโควตาแดงข้อ 4 หลายเท่า — ถ้าจะใช้ต้องแก้ที่โควตาแดงก่อน (v1.6) · `assets/reference-banner-lineup.jpg` เป็นภาพอ้างอิงภายใน ไม่ใช่ของขึ้นเว็บ

## ปมชื่อสินค้า — ปิดแล้วใน v1.6

**ผู้สั่งงานยืนยันให้ใช้ 5 ซีรีส์จริง ไม่ใช่ XT / CM / CL ตามบรีฟข้อ 1** — เป็นการต่างจากบรีฟโดยตั้งใจ `CL` ไม่มีอยู่จริงจึงหายไปจากเว็บทั้งหมด
`products.html` แสดงค่าจริงครบ 15 รหัส ตัวเลขทุกช่องถอดจาก `Data/films.json` และตรวจเทียบแล้วว่าตรงกันหมด **ถ้าแก้ค่าใดต้องแก้ที่ `films.json` ก่อนแล้วค่อยลงหน้าเว็บ ไม่ใช่แก้ที่ HTML ตรงๆ**

`#series` ในหน้าแรกที่เคยเขียน XT เป็น "Flagship" (ทั้งที่ EXTRA CLEAR ลดความร้อนต่ำที่สุดในไลน์) **ถูกลบทั้งบล็อกแล้ว** พร้อม CSS ที่ไม่มีใครใช้ — ค่าสมมติ 12 จุดที่เคยค้างตั้งแต่ v1.0 หายไปพร้อมบล็อกนี้

**ค่าที่ยังไม่มีที่มาบน `index.html`** — หน้าแรกไม่มี `{{VAR}}` เลยแม้แต่ตัวเดียว แต่ตัวนับ `.cnt` ยังขึ้น `3M+` ตารางฟุต · `99%` UV · `7 ปี` · `120+` ศูนย์ ทั้งที่ `{{SERVICE_CENTERS}}` ยังค้างอยู่ในรายการตัวแปร (มีแค่ `7 ปี` ที่ยืนยันแล้วใน v1.4) — **นี่คือช่องว่างที่ยังไม่ถูกจัดการ ไม่ใช่ของที่ผ่านแล้ว**

**ยังค้าง — `index.html:342` ยังอ้าง "ไม่มีชั้นโลหะในเนื้อฟิล์ม" ในระดับแบรนด์** ซึ่งไม่จริงกับ CRYSTALIZE ที่เป็น metalize sputtering · `products.html` เขียนถูกแล้วในระดับซีรีส์ ต้องแก้หน้าแรกให้ตรงกัน

**ส่วน `#work` ในหน้าแรกเป็นข้อมูลสมมติทั้งบล็อก** ถอดป้ายซีรีส์ออกแล้วเพราะมี `CL` ที่ไม่มีจริง แต่รถทั้งหกคันยังไม่ใช่ผลงานจริง — **จงใจไม่ใส่ชื่อซีรีส์จริงกลับเข้าไป** เพราะจะทำให้ข้อมูลสมมติดูน่าเชื่อขึ้นโดยที่ยังสมมติอยู่

**เนื้อหาเงื่อนไขใน `warranty.html` ยังเป็นโครงร่าง** มีบล็อกแจ้งกำกับบนหน้า — เป็นข้อความที่ผูกพันทางกฎหมาย หนักกว่าการแต่งตัวเลขสเปกตามข้อ 10 **ต้องแทนด้วยเอกสารจริงและลบบล็อกแจ้งก่อนขึ้นเซิร์ฟเวอร์**

**ราคาไม่ขึ้นเว็บโดยตั้งใจ** มีข้อมูลอยู่ใน `films.json` แค่ 3 ซีรีส์ และข้อ 1 ระบุว่าเว็บนี้ไม่ใช่ร้านค้า

## หนี้ที่ต้องแก้พร้อมกันทุกไฟล์

**ลำดับหัวข้อข้ามระดับ** — section ปิดท้ายเป็น `h2` แล้ว footer เป็น `h4` ต่อทันที ข้าม `h3` ซึ่งขัดข้อ 9 ตรงตัว มีมาตั้งแต่ v1.0 และคัดลอกต่อกันมาทุกไฟล์ (`faq.html` ไม่มี `h3` เลยทั้งหน้า) **ควรทำเป็นงานเดี่ยวรอบเดียวจบทั้ง 7 ไฟล์**
รายการค้างที่เหลือทั้งหมดอยู่ท้าย `docs/CHANGELOG.md` หัวข้อ "ค้างอยู่ — ต้องทำต่อ" (11 ข้อ) — ที่นั่นคือรายการจริง ไฟล์นี้ไม่เก็บซ้ำ

## วิธีทำงานที่คาดหวัง

ตาม `docs/master-prompt.md` ข้อ 12 — **ทวนโจทย์ให้ยืนยันก่อนเขียนโค้ดเสมอ ยกเว้นสั่งว่า "ทำเลย"** จากนั้นระบุโหมด A/B → อธิบายการตัดสินใจหลัก 3 ข้อพร้อมเหตุผล (ไม่ใช่รายการฟีเจอร์) → ส่งไฟล์ → แนบตัวแปรที่ต้องขอ → บอกตรงๆ ว่าจุดไหนยังอ่อน
ตอบเป็นภาษาไทย เนื้อหาบนเว็บเป็นภาษาไทยทั้งหมด (โครงสร้างต้องรองรับ EN ในเฟสถัดไป แต่ยังไม่ได้วาง)
ก่อนส่งงานต้องตรวจเกณฑ์ 9 ข้อ (ข้อ 11) ทีละข้อแล้วรายงานผล ไม่ใช่สรุปรวม — แล้วบันทึกลง `docs/CHANGELOG.md` พร้อมอัปเดตหัวข้อ "ค้างอยู่" และ "ตัวแปรที่ต้องขอ"
